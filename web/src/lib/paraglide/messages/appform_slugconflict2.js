/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{ slug: NonNullable<unknown>, other: NonNullable<unknown> }} Appform_Slugconflict2Inputs */

const en_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Same proxy path (/proxy/${i?.slug}/) as "${i?.other}". Rename one so their slugs differ.`)
};

const sv_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Samma proxysökväg (/proxy/${i?.slug}/) som "${i?.other}". Byt namn på en av dem så att sökvägarna skiljer sig åt.`)
};

const uk_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Такий самий шлях proxy (/proxy/${i?.slug}/), як у "${i?.other}". Перейменуйте один із застосунків, щоб шляхи відрізнялися.`)
};

const zh_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`与「${i?.other}」的代理路径（/proxy/${i?.slug}/）相同。请重命名其中一个，使两者的路径不同。`)
};

const es_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Misma ruta de proxy (/proxy/${i?.slug}/) que "${i?.other}". Renombra una para que sus slugs sean distintos.`)
};

const hi_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`"${i?.other}" के समान ही proxy पथ (/proxy/${i?.slug}/)। किसी एक का नाम बदलें ताकि उनके स्लग अलग हो जाएं।`)
};

const pt_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Mesmo caminho de proxy (/proxy/${i?.slug}/) que "${i?.other}". Renomeia uma delas para que os slugs sejam diferentes.`)
};

const bn_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`"${i?.other}"-এর মতো একই proxy পথ (/proxy/${i?.slug}/)। স্লাগ আলাদা করতে যেকোনো একটির নাম পরিবর্তন করুন।`)
};

const ru_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Тот же путь прокси (/proxy/${i?.slug}/), что и у "${i?.other}". Переименуйте одно из приложений, чтобы пути различались.`)
};

const ja_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`「${i?.other}」とプロキシパス（/proxy/${i?.slug}/）が同じです。スラッグが異なるように、どちらかの名前を変更してください。`)
};

const vi_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Trùng đường dẫn proxy (/proxy/${i?.slug}/) với "${i?.other}". Hãy đổi tên một trong hai để slug khác nhau.`)
};

const yue_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`同「${i?.other}」嘅代理路徑（/proxy/${i?.slug}/）一樣。改其中一個嘅名，令兩者嘅路徑唔同。`)
};

const tr_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`"${i?.other}" ile aynı proxy yolu (/proxy/${i?.slug}/). Slug'ların farklı olması için birini yeniden adlandırın.`)
};

const ar_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`نفس مسار proxy (/proxy/${i?.slug}/) الخاص بـ "${i?.other}". أعد تسمية أحدهما ليختلف المساران.`)
};

const wuu_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`和「${i?.other}」的代理路径（/proxy/${i?.slug}/）一样。改掉其中一个的名字，让两个路径不一样。`)
};

const mr_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`"${i?.other}" प्रमाणेच समान प्रॉक्सी मार्ग (/proxy/${i?.slug}/). स्लग वेगळे होण्यासाठी एकाचे नाव बदला.`)
};

const nb_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Samme proxysti (/proxy/${i?.slug}/) som "${i?.other}". Gi den ene et nytt navn, slik at stiene blir forskjellige.`)
};

const fi_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Sama proxy-polku (/proxy/${i?.slug}/) kuin sovelluksella "${i?.other}". Nimeä toinen uudelleen, jotta polut eroavat.`)
};

const da_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Samme proxysti (/proxy/${i?.slug}/) som "${i?.other}". Omdøb den ene, så stierne er forskellige.`)
};

const et_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Sama proxy tee (/proxy/${i?.slug}/) nagu rakendusel "${i?.other}". Nimetage üks ümber, et nende teed erineksid.`)
};

const lv_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Tāds pats proxy ceļš (/proxy/${i?.slug}/) kā lietotnei "${i?.other}". Pārdēvējiet vienu, lai to ceļi atšķirtos.`)
};

const lt_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Toks pat proxy kelias (/proxy/${i?.slug}/) kaip programos "${i?.other}". Pervadinkite vieną, kad jų keliai skirtųsi.`)
};

const pl_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Ta sama ścieżka proxy (/proxy/${i?.slug}/) co "${i?.other}". Zmień nazwę jednej z aplikacji, aby ścieżki się różniły.`)
};

const de_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Gleicher Proxy-Pfad (/proxy/${i?.slug}/) wie "${i?.other}". Benennen Sie eine davon um, damit sich die Pfade unterscheiden.`)
};

const nl_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Zelfde proxypad (/proxy/${i?.slug}/) als "${i?.other}". Hernoem er een zodat de paden verschillen.`)
};

const fr_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Même chemin proxy (/proxy/${i?.slug}/) que "${i?.other}". Renomme l'une des deux pour que leurs slugs diffèrent.`)
};

const it_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Stesso percorso proxy (/proxy/${i?.slug}/) di "${i?.other}". Rinomina una delle due in modo che gli slug siano diversi.`)
};

const hu_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Ugyanaz a proxy útvonal (/proxy/${i?.slug}/), mint a következőé: "${i?.other}". Nevezze át az egyiket, hogy az útvonalaik eltérjenek.`)
};

const cs_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Stejná cesta proxy (/proxy/${i?.slug}/) jako "${i?.other}". Přejmenujte jednu z aplikací, aby se cesty lišily.`)
};

const ro_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Aceeași cale proxy (/proxy/${i?.slug}/) ca "${i?.other}". Redenumește una dintre ele astfel încât slug-urile să difere.`)
};

const el_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Ίδια διαδρομή proxy (/proxy/${i?.slug}/) με την εφαρμογή "${i?.other}". Μετονομάστε τη μία ώστε οι διαδρομές τους να διαφέρουν.`)
};

const bg_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Същият proxy път (/proxy/${i?.slug}/) като "${i?.other}". Преименувай едното приложение, за да се различават пътищата.`)
};

const hr_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Ista proxy putanja (/proxy/${i?.slug}/) kao aplikacija "${i?.other}". Preimenujte jednu kako bi im se putanje razlikovale.`)
};

const sr_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Ista proxy putanja (/proxy/${i?.slug}/) kao aplikacija "${i?.other}". Preimenujte jednu da bi im se putanje razlikovale.`)
};

const sk_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Rovnaká cesta proxy (/proxy/${i?.slug}/) ako "${i?.other}". Premenujte jednu z aplikácií, aby sa cesty líšili.`)
};

const sl_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Enaka proxy pot (/proxy/${i?.slug}/) kot aplikacija "${i?.other}". Eno preimenujte, da se poti razlikujeta.`)
};

const ko_appform_slugconflict2 = /** @type {(inputs: Appform_Slugconflict2Inputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`"${i?.other}"와(과) 프록시 경로(/proxy/${i?.slug}/)가 같습니다. 슬러그가 다르도록 하나를 이름 변경하세요.`)
};

/**
* | output |
* | --- |
* | "Same proxy path (/proxy/{slug}/) as \"{other}\". Rename one so their slugs differ." |
*
* @param {Appform_Slugconflict2Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
const appform_slugconflict2 = /** @type {((inputs: Appform_Slugconflict2Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Slugconflict2Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "sv") return sv_appform_slugconflict2(inputs)
	if (locale === "uk") return uk_appform_slugconflict2(inputs)
	if (locale === "zh") return zh_appform_slugconflict2(inputs)
	if (locale === "es") return es_appform_slugconflict2(inputs)
	if (locale === "hi") return hi_appform_slugconflict2(inputs)
	if (locale === "pt") return pt_appform_slugconflict2(inputs)
	if (locale === "bn") return bn_appform_slugconflict2(inputs)
	if (locale === "ru") return ru_appform_slugconflict2(inputs)
	if (locale === "ja") return ja_appform_slugconflict2(inputs)
	if (locale === "vi") return vi_appform_slugconflict2(inputs)
	if (locale === "yue") return yue_appform_slugconflict2(inputs)
	if (locale === "tr") return tr_appform_slugconflict2(inputs)
	if (locale === "ar") return ar_appform_slugconflict2(inputs)
	if (locale === "wuu") return wuu_appform_slugconflict2(inputs)
	if (locale === "mr") return mr_appform_slugconflict2(inputs)
	if (locale === "nb") return nb_appform_slugconflict2(inputs)
	if (locale === "fi") return fi_appform_slugconflict2(inputs)
	if (locale === "da") return da_appform_slugconflict2(inputs)
	if (locale === "et") return et_appform_slugconflict2(inputs)
	if (locale === "lv") return lv_appform_slugconflict2(inputs)
	if (locale === "lt") return lt_appform_slugconflict2(inputs)
	if (locale === "pl") return pl_appform_slugconflict2(inputs)
	if (locale === "de") return de_appform_slugconflict2(inputs)
	if (locale === "nl") return nl_appform_slugconflict2(inputs)
	if (locale === "fr") return fr_appform_slugconflict2(inputs)
	if (locale === "it") return it_appform_slugconflict2(inputs)
	if (locale === "hu") return hu_appform_slugconflict2(inputs)
	if (locale === "cs") return cs_appform_slugconflict2(inputs)
	if (locale === "ro") return ro_appform_slugconflict2(inputs)
	if (locale === "el") return el_appform_slugconflict2(inputs)
	if (locale === "bg") return bg_appform_slugconflict2(inputs)
	if (locale === "hr") return hr_appform_slugconflict2(inputs)
	if (locale === "sr") return sr_appform_slugconflict2(inputs)
	if (locale === "sk") return sk_appform_slugconflict2(inputs)
	if (locale === "sl") return sl_appform_slugconflict2(inputs)
	if (locale === "ko") return ko_appform_slugconflict2(inputs)
	return en_appform_slugconflict2(inputs)
});
export { appform_slugconflict2 as "appForm_slugConflict" }