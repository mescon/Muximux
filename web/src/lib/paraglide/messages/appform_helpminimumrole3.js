/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Helpminimumrole3Inputs */

const en_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const sv_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const uk_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const zh_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const es_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const hi_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const pt_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const bn_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const ru_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const ja_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const pa_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const vi_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const yue_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const tr_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const ar_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const wuu_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const mr_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const nb_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const fi_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const da_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const et_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const lv_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const lt_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const pl_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const de_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const nl_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const fr_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const it_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const hu_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const cs_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const ro_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const el_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const bg_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const hr_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const sr_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const sk_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

const sl_appform_helpminimumrole3 = /** @type {(inputs: Appform_Helpminimumrole3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses.`)
};

/**
* | output |
* | --- |
* | "Restricts visibility based on user role. Users below the selected role won't see this app in the navigation or API responses." |
*
* @param {Appform_Helpminimumrole3Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const appform_helpminimumrole3 = /** @type {((inputs?: Appform_Helpminimumrole3Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Helpminimumrole3Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_appform_helpminimumrole3(inputs)
	if (locale === "sv") return sv_appform_helpminimumrole3(inputs)
	if (locale === "uk") return uk_appform_helpminimumrole3(inputs)
	if (locale === "zh") return zh_appform_helpminimumrole3(inputs)
	if (locale === "es") return es_appform_helpminimumrole3(inputs)
	if (locale === "hi") return hi_appform_helpminimumrole3(inputs)
	if (locale === "pt") return pt_appform_helpminimumrole3(inputs)
	if (locale === "bn") return bn_appform_helpminimumrole3(inputs)
	if (locale === "ru") return ru_appform_helpminimumrole3(inputs)
	if (locale === "ja") return ja_appform_helpminimumrole3(inputs)
	if (locale === "pa") return pa_appform_helpminimumrole3(inputs)
	if (locale === "vi") return vi_appform_helpminimumrole3(inputs)
	if (locale === "yue") return yue_appform_helpminimumrole3(inputs)
	if (locale === "tr") return tr_appform_helpminimumrole3(inputs)
	if (locale === "ar") return ar_appform_helpminimumrole3(inputs)
	if (locale === "wuu") return wuu_appform_helpminimumrole3(inputs)
	if (locale === "mr") return mr_appform_helpminimumrole3(inputs)
	if (locale === "nb") return nb_appform_helpminimumrole3(inputs)
	if (locale === "fi") return fi_appform_helpminimumrole3(inputs)
	if (locale === "da") return da_appform_helpminimumrole3(inputs)
	if (locale === "et") return et_appform_helpminimumrole3(inputs)
	if (locale === "lv") return lv_appform_helpminimumrole3(inputs)
	if (locale === "lt") return lt_appform_helpminimumrole3(inputs)
	if (locale === "pl") return pl_appform_helpminimumrole3(inputs)
	if (locale === "de") return de_appform_helpminimumrole3(inputs)
	if (locale === "nl") return nl_appform_helpminimumrole3(inputs)
	if (locale === "fr") return fr_appform_helpminimumrole3(inputs)
	if (locale === "it") return it_appform_helpminimumrole3(inputs)
	if (locale === "hu") return hu_appform_helpminimumrole3(inputs)
	if (locale === "cs") return cs_appform_helpminimumrole3(inputs)
	if (locale === "ro") return ro_appform_helpminimumrole3(inputs)
	if (locale === "el") return el_appform_helpminimumrole3(inputs)
	if (locale === "bg") return bg_appform_helpminimumrole3(inputs)
	if (locale === "hr") return hr_appform_helpminimumrole3(inputs)
	if (locale === "sr") return sr_appform_helpminimumrole3(inputs)
	if (locale === "sk") return sk_appform_helpminimumrole3(inputs)
	return sl_appform_helpminimumrole3(inputs)
});
export { appform_helpminimumrole3 as "appForm_helpMinimumRole" }