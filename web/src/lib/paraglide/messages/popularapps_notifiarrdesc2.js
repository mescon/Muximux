/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Popularapps_Notifiarrdesc2Inputs */

const en_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Notification routing for *arr stacks`)
};

const sv_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Aviseringsrutting för *arr-stackar`)
};

const uk_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Маршрутизація сповіщень для *arr стеків`)
};

const zh_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`面向 *arr 堆栈的通知路由`)
};

const es_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Enrutamiento de notificaciones para stacks *arr`)
};

const hi_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`*arr स्टैक के लिए नोटिफिकेशन रूटिंग`)
};

const pt_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Encaminhamento de notificações para stacks *arr`)
};

const bn_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`*arr স্ট্যাকের জন্য নোটিফিকেশন রাউটিং`)
};

const ru_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Маршрутизация уведомлений для стека *arr`)
};

const ja_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`*arr スタック向けの通知ルーティング`)
};

const vi_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Định tuyến thông báo cho stack *arr`)
};

const yue_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`為 *arr 堆疊做通知路由`)
};

const tr_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`*arr yığınları için bildirim yönlendirme`)
};

const ar_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`توجيه الإشعارات لحِزم *arr`)
};

const wuu_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`为 *arr 栈做通知路由`)
};

const mr_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`*arr स्टॅकसाठी सूचना राउटिंग`)
};

const nb_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Varselruting for *arr-stacker`)
};

const fi_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Ilmoitusten reititys *arr-pinoille`)
};

const da_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Notifikationsrouting for *arr-stacks`)
};

const et_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Teadete marsruutimine *arr pinude jaoks`)
};

const lv_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Paziņojumu maršrutēšana *arr kaudzēm`)
};

const lt_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pranešimų nukreipimas *arr dėklams`)
};

const pl_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Routing powiadomień dla stosów *arr`)
};

const de_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Benachrichtigungsweiterleitung für *arr-Stacks`)
};

const nl_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Notificatierouting voor *arr-stacks`)
};

const fr_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Routage des notifications pour les stacks *arr`)
};

const it_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Instradamento delle notifiche per gli stack *arr`)
};

const hu_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Értesítések irányítása *arr veremekhez`)
};

const cs_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Směrování oznámení pro stacky *arr`)
};

const ro_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Rutare a notificărilor pentru stivele *arr`)
};

const el_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Δρομολόγηση ειδοποιήσεων για στοίβες *arr`)
};

const bg_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Маршрутизиране на известия за *arr стекове`)
};

const hr_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Usmjeravanje obavijesti za *arr složaje`)
};

const sr_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Usmeravanje obaveštenja za *arr stekove`)
};

const sk_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Smerovanie oznámení pre stacky *arr`)
};

const sl_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Usmerjanje obvestil za sklade *arr`)
};

const ko_popularapps_notifiarrdesc2 = /** @type {(inputs: Popularapps_Notifiarrdesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`*arr 스택용 알림 라우팅`)
};

/**
* | output |
* | --- |
* | "Notification routing for *arr stacks" |
*
* @param {Popularapps_Notifiarrdesc2Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
const popularapps_notifiarrdesc2 = /** @type {((inputs?: Popularapps_Notifiarrdesc2Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Popularapps_Notifiarrdesc2Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_popularapps_notifiarrdesc2(inputs)
	if (locale === "sv") return sv_popularapps_notifiarrdesc2(inputs)
	if (locale === "uk") return uk_popularapps_notifiarrdesc2(inputs)
	if (locale === "zh") return zh_popularapps_notifiarrdesc2(inputs)
	if (locale === "es") return es_popularapps_notifiarrdesc2(inputs)
	if (locale === "hi") return hi_popularapps_notifiarrdesc2(inputs)
	if (locale === "pt") return pt_popularapps_notifiarrdesc2(inputs)
	if (locale === "bn") return bn_popularapps_notifiarrdesc2(inputs)
	if (locale === "ru") return ru_popularapps_notifiarrdesc2(inputs)
	if (locale === "ja") return ja_popularapps_notifiarrdesc2(inputs)
	if (locale === "vi") return vi_popularapps_notifiarrdesc2(inputs)
	if (locale === "yue") return yue_popularapps_notifiarrdesc2(inputs)
	if (locale === "tr") return tr_popularapps_notifiarrdesc2(inputs)
	if (locale === "ar") return ar_popularapps_notifiarrdesc2(inputs)
	if (locale === "wuu") return wuu_popularapps_notifiarrdesc2(inputs)
	if (locale === "mr") return mr_popularapps_notifiarrdesc2(inputs)
	if (locale === "nb") return nb_popularapps_notifiarrdesc2(inputs)
	if (locale === "fi") return fi_popularapps_notifiarrdesc2(inputs)
	if (locale === "da") return da_popularapps_notifiarrdesc2(inputs)
	if (locale === "et") return et_popularapps_notifiarrdesc2(inputs)
	if (locale === "lv") return lv_popularapps_notifiarrdesc2(inputs)
	if (locale === "lt") return lt_popularapps_notifiarrdesc2(inputs)
	if (locale === "pl") return pl_popularapps_notifiarrdesc2(inputs)
	if (locale === "de") return de_popularapps_notifiarrdesc2(inputs)
	if (locale === "nl") return nl_popularapps_notifiarrdesc2(inputs)
	if (locale === "fr") return fr_popularapps_notifiarrdesc2(inputs)
	if (locale === "it") return it_popularapps_notifiarrdesc2(inputs)
	if (locale === "hu") return hu_popularapps_notifiarrdesc2(inputs)
	if (locale === "cs") return cs_popularapps_notifiarrdesc2(inputs)
	if (locale === "ro") return ro_popularapps_notifiarrdesc2(inputs)
	if (locale === "el") return el_popularapps_notifiarrdesc2(inputs)
	if (locale === "bg") return bg_popularapps_notifiarrdesc2(inputs)
	if (locale === "hr") return hr_popularapps_notifiarrdesc2(inputs)
	if (locale === "sr") return sr_popularapps_notifiarrdesc2(inputs)
	if (locale === "sk") return sk_popularapps_notifiarrdesc2(inputs)
	if (locale === "sl") return sl_popularapps_notifiarrdesc2(inputs)
	return ko_popularapps_notifiarrdesc2(inputs)
});
export { popularapps_notifiarrdesc2 as "popularApps_notifiarrDesc" }