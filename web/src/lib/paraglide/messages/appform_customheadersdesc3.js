/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Customheadersdesc3Inputs */

const en_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sent to the backend on every proxied request (e.g. Authorization, X-Api-Key). Use \${user}, \${role}, \${email}, \${display_name}, or \${groups} in a value to forward the signed-in user's identity.`)
};

const sv_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Skickas till backend vid varje proxad förfrågan (t.ex. Authorization, X-Api-Key). Använd \${user}, \${role}, \${email}, \${display_name} eller \${groups} i ett värde för att vidarebefordra den inloggade användarens identitet.`)
};

const uk_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Надсилаються на backend з кожним проксованим запитом (напр. Authorization, X-Api-Key). Використовуйте \${user}, \${role}, \${email}, \${display_name} або \${groups} у значенні, щоб передати дані користувача, який увійшов.`)
};

const zh_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`在每次代理请求时发送到后端（例如 Authorization、X-Api-Key）。在值中使用 \${user}、\${role}、\${email}、\${display_name} 或 \${groups}，即可转发已登录用户的身份信息。`)
};

const es_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Se envían al backend en cada solicitud a través del proxy (ej. Authorization, X-Api-Key). Usa \${user}, \${role}, \${email}, \${display_name} o \${groups} en un valor para reenviar la identidad del usuario que ha iniciado sesión.`)
};

const hi_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`हर प्रॉक्सी किए गए अनुरोध पर बैकएंड को भेजे जाते हैं (जैसे Authorization, X-Api-Key)। साइन इन किए गए उपयोगकर्ता की पहचान भेजने के लिए मान में \${user}, \${role}, \${email}, \${display_name} या \${groups} का उपयोग करें।`)
};

const pt_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Enviados ao backend em cada pedido via proxy (ex. Authorization, X-Api-Key). Use \${user}, \${role}, \${email}, \${display_name} ou \${groups} num valor para encaminhar a identidade do utilizador com sessão iniciada.`)
};

const bn_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`প্রতিটি প্রক্সি করা অনুরোধে ব্যাকএন্ডে পাঠানো হয় (যেমন Authorization, X-Api-Key)। সাইন ইন করা ব্যবহারকারীর পরিচয় ফরওয়ার্ড করতে মানের মধ্যে \${user}, \${role}, \${email}, \${display_name} বা \${groups} ব্যবহার করুন।`)
};

const ru_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Отправляются серверу при каждом проксируемом запросе (например, Authorization, X-Api-Key). Используйте \${user}, \${role}, \${email}, \${display_name} или \${groups} в значении, чтобы передать данные вошедшего пользователя.`)
};

const ja_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`プロキシリクエスト時にバックエンドに送信（例: Authorization、X-Api-Key）。値に \${user}、\${role}、\${email}、\${display_name}、\${groups} を使うと、サインイン中のユーザー情報を転送できます。`)
};

const vi_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gửi đến backend trong mỗi yêu cầu qua proxy (vd: Authorization, X-Api-Key). Dùng \${user}, \${role}, \${email}, \${display_name} hoặc \${groups} trong giá trị để chuyển tiếp danh tính của người dùng đã đăng nhập.`)
};

const yue_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`喺每次代理請求時發送到後端（例如 Authorization、X-Api-Key）。喺值入面用 \${user}、\${role}、\${email}、\${display_name} 或者 \${groups}，就可以轉發已登入使用者嘅身份資料。`)
};

const tr_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Her proxy isteğinde arka uca gönderilir (ör. Authorization, X-Api-Key). Oturum açmış kullanıcının kimliğini iletmek için bir değerde \${user}, \${role}, \${email}, \${display_name} veya \${groups} kullanın.`)
};

const ar_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`تُرسل إلى الخادم مع كل طلب عبر proxy (مثل Authorization، X-Api-Key). استخدم \${user}، \${role}، \${email}، \${display_name} أو \${groups} في القيمة لتمرير هوية المستخدم الذي سجّل الدخول.`)
};

const wuu_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`每次代理请求时发送给后端（如 Authorization、X-Api-Key）。勒值里向用 \${user}、\${role}、\${email}、\${display_name} 或者 \${groups}，可以把已登录用户个身份信息转发过去。`)
};

const mr_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`प्रत्येक प्रॉक्सी केलेल्या विनंतीवर बॅकएंडला पाठवले जातात (उदा. Authorization, X-Api-Key). साइन इन केलेल्या वापरकर्त्याची ओळख पाठवण्यासाठी मूल्यात \${user}, \${role}, \${email}, \${display_name} किंवा \${groups} वापरा.`)
};

const nb_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sendes til backend ved hver proxyet forespørsel (f.eks. Authorization, X-Api-Key). Bruk \${user}, \${role}, \${email}, \${display_name} eller \${groups} i en verdi for å videresende den innloggede brukerens identitet.`)
};

const fi_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lähetetään taustajärjestelmälle jokaisessa proxy-pyynnössä (esim. Authorization, X-Api-Key). Käytä arvossa \${user}, \${role}, \${email}, \${display_name} tai \${groups} välittääksesi kirjautuneen käyttäjän henkilöllisyyden.`)
};

const da_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sendes til backend ved hver proxyet forespørgsel (f.eks. Authorization, X-Api-Key). Brug \${user}, \${role}, \${email}, \${display_name} eller \${groups} i en værdi for at videresende den indloggede brugers identitet.`)
};

const et_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saadetakse tagarakendusele igal proxy päringul (nt Authorization, X-Api-Key). Kasuta väärtuses \${user}, \${role}, \${email}, \${display_name} või \${groups}, et edastada sisselogitud kasutaja identiteet.`)
};

const lv_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Tiek sūtītas aizmugursistēmai ar katru proxy pieprasījumu (piem. Authorization, X-Api-Key). Izmantojiet vērtībā \${user}, \${role}, \${email}, \${display_name} vai \${groups}, lai pārsūtītu pieteikušos lietotāja identitāti.`)
};

const lt_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Siunčiamos backend su kiekviena proxy užklausa (pvz., Authorization, X-Api-Key). Reikšmėje naudokite \${user}, \${role}, \${email}, \${display_name} arba \${groups}, kad perduotumėte prisijungusio naudotojo tapatybę.`)
};

const pl_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Wysyłane do backendu przy każdym żądaniu proxy (np. Authorization, X-Api-Key). Użyj w wartości \${user}, \${role}, \${email}, \${display_name} lub \${groups}, aby przekazać tożsamość zalogowanego użytkownika.`)
};

const de_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Werden bei jeder Proxy-Anfrage an das Backend gesendet (z. B. Authorization, X-Api-Key). Verwenden Sie \${user}, \${role}, \${email}, \${display_name} oder \${groups} in einem Wert, um die Identität des angemeldeten Benutzers weiterzuleiten.`)
};

const nl_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Worden bij elk proxy-verzoek naar de backend gestuurd (bijv. Authorization, X-Api-Key). Gebruik \${user}, \${role}, \${email}, \${display_name} of \${groups} in een waarde om de identiteit van de ingelogde gebruiker door te sturen.`)
};

const fr_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Envoyés au backend à chaque requête proxy (ex : Authorization, X-Api-Key). Utilisez \${user}, \${role}, \${email}, \${display_name} ou \${groups} dans une valeur pour transmettre l'identité de l'utilisateur connecté.`)
};

const it_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inviati al backend ad ogni richiesta proxy (es. Authorization, X-Api-Key). Usa \${user}, \${role}, \${email}, \${display_name} o \${groups} in un valore per inoltrare l'identità dell'utente connesso.`)
};

const hu_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Minden proxy kéréskor elküldve a backendnek (pl. Authorization, X-Api-Key). Használja a \${user}, \${role}, \${email}, \${display_name} vagy \${groups} változót egy értékben a bejelentkezett felhasználó azonosságának továbbításához.`)
};

const cs_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Odesílány na backend při každém proxy požadavku (např. Authorization, X-Api-Key). Použijte v hodnotě \${user}, \${role}, \${email}, \${display_name} nebo \${groups} k předání identity přihlášeného uživatele.`)
};

const ro_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Trimise la backend la fiecare cerere proxiată (ex. Authorization, X-Api-Key). Folosiți \${user}, \${role}, \${email}, \${display_name} sau \${groups} într-o valoare pentru a transmite identitatea utilizatorului autentificat.`)
};

const el_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Αποστέλλονται στο backend σε κάθε αίτημα μέσω proxy (π.χ. Authorization, X-Api-Key). Χρησιμοποιήστε \${user}, \${role}, \${email}, \${display_name} ή \${groups} σε μια τιμή για να προωθήσετε την ταυτότητα του συνδεδεμένου χρήστη.`)
};

const bg_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Изпращат се към бекенда при всяка проксирана заявка (напр. Authorization, X-Api-Key). Използвайте \${user}, \${role}, \${email}, \${display_name} или \${groups} в стойността, за да предадете самоличността на влезлия потребител.`)
};

const hr_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Šalju se pozadinskom sustavu pri svakom proxiranom zahtjevu (npr. Authorization, X-Api-Key). U vrijednosti upotrijebite \${user}, \${role}, \${email}, \${display_name} ili \${groups} za prosljeđivanje identiteta prijavljenog korisnika.`)
};

const sr_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Šalju se pozadinskom sistemu pri svakom proxiranom zahtevu (npr. Authorization, X-Api-Key). U vrednosti upotrebite \${user}, \${role}, \${email}, \${display_name} ili \${groups} za prosleđivanje identiteta prijavljenog korisnika.`)
};

const sk_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Odosielané na backend pri každom proxy požiadavku (napr. Authorization, X-Api-Key). Použite v hodnote \${user}, \${role}, \${email}, \${display_name} alebo \${groups} na odovzdanie identity prihláseného používateľa.`)
};

const sl_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pošiljajo se zalednemu sistemu ob vsaki proxirani zahtevi (npr. Authorization, X-Api-Key). V vrednosti uporabite \${user}, \${role}, \${email}, \${display_name} ali \${groups} za posredovanje identitete prijavljenega uporabnika.`)
};

const ko_appform_customheadersdesc3 = /** @type {(inputs: Appform_Customheadersdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`모든 프록시 요청 시 백엔드로 전송됩니다 (예: Authorization, X-Api-Key). 값에서 \${user}, \${role}, \${email}, \${display_name}, 또는 \${groups}를 사용하여 로그인한 사용자의 신원을 전달할 수 있습니다.`)
};

/**
* | output |
* | --- |
* | "Sent to the backend on every proxied request (e.g. Authorization, X-Api-Key). Use ${user}, ${role}, ${email}, ${display_name}, or ${groups} in a value to for..." |
*
* @param {Appform_Customheadersdesc3Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
const appform_customheadersdesc3 = /** @type {((inputs?: Appform_Customheadersdesc3Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Customheadersdesc3Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "sv") return sv_appform_customheadersdesc3(inputs)
	if (locale === "uk") return uk_appform_customheadersdesc3(inputs)
	if (locale === "zh") return zh_appform_customheadersdesc3(inputs)
	if (locale === "es") return es_appform_customheadersdesc3(inputs)
	if (locale === "hi") return hi_appform_customheadersdesc3(inputs)
	if (locale === "pt") return pt_appform_customheadersdesc3(inputs)
	if (locale === "bn") return bn_appform_customheadersdesc3(inputs)
	if (locale === "ru") return ru_appform_customheadersdesc3(inputs)
	if (locale === "ja") return ja_appform_customheadersdesc3(inputs)
	if (locale === "vi") return vi_appform_customheadersdesc3(inputs)
	if (locale === "yue") return yue_appform_customheadersdesc3(inputs)
	if (locale === "tr") return tr_appform_customheadersdesc3(inputs)
	if (locale === "ar") return ar_appform_customheadersdesc3(inputs)
	if (locale === "wuu") return wuu_appform_customheadersdesc3(inputs)
	if (locale === "mr") return mr_appform_customheadersdesc3(inputs)
	if (locale === "nb") return nb_appform_customheadersdesc3(inputs)
	if (locale === "fi") return fi_appform_customheadersdesc3(inputs)
	if (locale === "da") return da_appform_customheadersdesc3(inputs)
	if (locale === "et") return et_appform_customheadersdesc3(inputs)
	if (locale === "lv") return lv_appform_customheadersdesc3(inputs)
	if (locale === "lt") return lt_appform_customheadersdesc3(inputs)
	if (locale === "pl") return pl_appform_customheadersdesc3(inputs)
	if (locale === "de") return de_appform_customheadersdesc3(inputs)
	if (locale === "nl") return nl_appform_customheadersdesc3(inputs)
	if (locale === "fr") return fr_appform_customheadersdesc3(inputs)
	if (locale === "it") return it_appform_customheadersdesc3(inputs)
	if (locale === "hu") return hu_appform_customheadersdesc3(inputs)
	if (locale === "cs") return cs_appform_customheadersdesc3(inputs)
	if (locale === "ro") return ro_appform_customheadersdesc3(inputs)
	if (locale === "el") return el_appform_customheadersdesc3(inputs)
	if (locale === "bg") return bg_appform_customheadersdesc3(inputs)
	if (locale === "hr") return hr_appform_customheadersdesc3(inputs)
	if (locale === "sr") return sr_appform_customheadersdesc3(inputs)
	if (locale === "sk") return sk_appform_customheadersdesc3(inputs)
	if (locale === "sl") return sl_appform_customheadersdesc3(inputs)
	if (locale === "ko") return ko_appform_customheadersdesc3(inputs)
	return en_appform_customheadersdesc3(inputs)
});
export { appform_customheadersdesc3 as "appForm_customHeadersDesc" }