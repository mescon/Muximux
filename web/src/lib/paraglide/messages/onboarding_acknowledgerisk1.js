/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Onboarding_Acknowledgerisk1Inputs */

const en_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const sv_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const uk_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const zh_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const es_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const hi_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const pt_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const bn_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const ru_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const ja_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const pa_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const vi_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const yue_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const tr_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const ar_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const wuu_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const mr_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const nb_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const fi_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const da_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const et_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const lv_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const lt_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const pl_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const de_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const nl_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const fr_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const it_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const hu_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const cs_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const ro_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const el_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const bg_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const hr_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const sr_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const sk_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

const sl_onboarding_acknowledgerisk1 = /** @type {(inputs: Onboarding_Acknowledgerisk1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`I understand the risks and want to proceed without authentication`)
};

/**
* | output |
* | --- |
* | "I understand the risks and want to proceed without authentication" |
*
* @param {Onboarding_Acknowledgerisk1Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const onboarding_acknowledgerisk1 = /** @type {((inputs?: Onboarding_Acknowledgerisk1Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Onboarding_Acknowledgerisk1Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_onboarding_acknowledgerisk1(inputs)
	if (locale === "sv") return sv_onboarding_acknowledgerisk1(inputs)
	if (locale === "uk") return uk_onboarding_acknowledgerisk1(inputs)
	if (locale === "zh") return zh_onboarding_acknowledgerisk1(inputs)
	if (locale === "es") return es_onboarding_acknowledgerisk1(inputs)
	if (locale === "hi") return hi_onboarding_acknowledgerisk1(inputs)
	if (locale === "pt") return pt_onboarding_acknowledgerisk1(inputs)
	if (locale === "bn") return bn_onboarding_acknowledgerisk1(inputs)
	if (locale === "ru") return ru_onboarding_acknowledgerisk1(inputs)
	if (locale === "ja") return ja_onboarding_acknowledgerisk1(inputs)
	if (locale === "pa") return pa_onboarding_acknowledgerisk1(inputs)
	if (locale === "vi") return vi_onboarding_acknowledgerisk1(inputs)
	if (locale === "yue") return yue_onboarding_acknowledgerisk1(inputs)
	if (locale === "tr") return tr_onboarding_acknowledgerisk1(inputs)
	if (locale === "ar") return ar_onboarding_acknowledgerisk1(inputs)
	if (locale === "wuu") return wuu_onboarding_acknowledgerisk1(inputs)
	if (locale === "mr") return mr_onboarding_acknowledgerisk1(inputs)
	if (locale === "nb") return nb_onboarding_acknowledgerisk1(inputs)
	if (locale === "fi") return fi_onboarding_acknowledgerisk1(inputs)
	if (locale === "da") return da_onboarding_acknowledgerisk1(inputs)
	if (locale === "et") return et_onboarding_acknowledgerisk1(inputs)
	if (locale === "lv") return lv_onboarding_acknowledgerisk1(inputs)
	if (locale === "lt") return lt_onboarding_acknowledgerisk1(inputs)
	if (locale === "pl") return pl_onboarding_acknowledgerisk1(inputs)
	if (locale === "de") return de_onboarding_acknowledgerisk1(inputs)
	if (locale === "nl") return nl_onboarding_acknowledgerisk1(inputs)
	if (locale === "fr") return fr_onboarding_acknowledgerisk1(inputs)
	if (locale === "it") return it_onboarding_acknowledgerisk1(inputs)
	if (locale === "hu") return hu_onboarding_acknowledgerisk1(inputs)
	if (locale === "cs") return cs_onboarding_acknowledgerisk1(inputs)
	if (locale === "ro") return ro_onboarding_acknowledgerisk1(inputs)
	if (locale === "el") return el_onboarding_acknowledgerisk1(inputs)
	if (locale === "bg") return bg_onboarding_acknowledgerisk1(inputs)
	if (locale === "hr") return hr_onboarding_acknowledgerisk1(inputs)
	if (locale === "sr") return sr_onboarding_acknowledgerisk1(inputs)
	if (locale === "sk") return sk_onboarding_acknowledgerisk1(inputs)
	return sl_onboarding_acknowledgerisk1(inputs)
});
export { onboarding_acknowledgerisk1 as "onboarding_acknowledgeRisk" }