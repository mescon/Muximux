/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Helphealthcheck3Inputs */

const en_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const sv_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const uk_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const zh_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const es_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const hi_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const pt_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const bn_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const ru_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const ja_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const pa_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const vi_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const yue_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const tr_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const ar_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const wuu_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const mr_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const nb_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const fi_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const da_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const et_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const lv_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const lt_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const pl_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const de_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const nl_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const fr_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const it_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const hu_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const cs_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const ro_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const el_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const bg_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const hr_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const sr_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const sk_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

const sl_appform_helphealthcheck3 = /** @type {(inputs: Appform_Helphealthcheck3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation.`)
};

/**
* | output |
* | --- |
* | "Periodically pings the app URL (or health URL if set) and shows a status indicator in the navigation." |
*
* @param {Appform_Helphealthcheck3Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const appform_helphealthcheck3 = /** @type {((inputs?: Appform_Helphealthcheck3Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Helphealthcheck3Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_appform_helphealthcheck3(inputs)
	if (locale === "sv") return sv_appform_helphealthcheck3(inputs)
	if (locale === "uk") return uk_appform_helphealthcheck3(inputs)
	if (locale === "zh") return zh_appform_helphealthcheck3(inputs)
	if (locale === "es") return es_appform_helphealthcheck3(inputs)
	if (locale === "hi") return hi_appform_helphealthcheck3(inputs)
	if (locale === "pt") return pt_appform_helphealthcheck3(inputs)
	if (locale === "bn") return bn_appform_helphealthcheck3(inputs)
	if (locale === "ru") return ru_appform_helphealthcheck3(inputs)
	if (locale === "ja") return ja_appform_helphealthcheck3(inputs)
	if (locale === "pa") return pa_appform_helphealthcheck3(inputs)
	if (locale === "vi") return vi_appform_helphealthcheck3(inputs)
	if (locale === "yue") return yue_appform_helphealthcheck3(inputs)
	if (locale === "tr") return tr_appform_helphealthcheck3(inputs)
	if (locale === "ar") return ar_appform_helphealthcheck3(inputs)
	if (locale === "wuu") return wuu_appform_helphealthcheck3(inputs)
	if (locale === "mr") return mr_appform_helphealthcheck3(inputs)
	if (locale === "nb") return nb_appform_helphealthcheck3(inputs)
	if (locale === "fi") return fi_appform_helphealthcheck3(inputs)
	if (locale === "da") return da_appform_helphealthcheck3(inputs)
	if (locale === "et") return et_appform_helphealthcheck3(inputs)
	if (locale === "lv") return lv_appform_helphealthcheck3(inputs)
	if (locale === "lt") return lt_appform_helphealthcheck3(inputs)
	if (locale === "pl") return pl_appform_helphealthcheck3(inputs)
	if (locale === "de") return de_appform_helphealthcheck3(inputs)
	if (locale === "nl") return nl_appform_helphealthcheck3(inputs)
	if (locale === "fr") return fr_appform_helphealthcheck3(inputs)
	if (locale === "it") return it_appform_helphealthcheck3(inputs)
	if (locale === "hu") return hu_appform_helphealthcheck3(inputs)
	if (locale === "cs") return cs_appform_helphealthcheck3(inputs)
	if (locale === "ro") return ro_appform_helphealthcheck3(inputs)
	if (locale === "el") return el_appform_helphealthcheck3(inputs)
	if (locale === "bg") return bg_appform_helphealthcheck3(inputs)
	if (locale === "hr") return hr_appform_helphealthcheck3(inputs)
	if (locale === "sr") return sr_appform_helphealthcheck3(inputs)
	if (locale === "sk") return sk_appform_helphealthcheck3(inputs)
	return sl_appform_helphealthcheck3(inputs)
});
export { appform_helphealthcheck3 as "appForm_helpHealthCheck" }