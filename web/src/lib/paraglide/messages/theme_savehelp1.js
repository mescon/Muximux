/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Theme_Savehelp1Inputs */

const en_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const sv_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const uk_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const zh_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const es_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const hi_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const pt_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const bn_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const ru_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const ja_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const pa_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const vi_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const yue_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const tr_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const ar_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const wuu_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const mr_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const nb_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const fi_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const da_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const et_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const lv_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const lt_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const pl_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const de_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const nl_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const fr_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const it_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const hu_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const cs_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const ro_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const el_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const bg_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const hr_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const sr_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const sk_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

const sl_theme_savehelp1 = /** @type {(inputs: Theme_Savehelp1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saves as a CSS file on the server. Changes are live-previewed above.`)
};

/**
* | output |
* | --- |
* | "Saves as a CSS file on the server. Changes are live-previewed above." |
*
* @param {Theme_Savehelp1Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const theme_savehelp1 = /** @type {((inputs?: Theme_Savehelp1Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Theme_Savehelp1Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_theme_savehelp1(inputs)
	if (locale === "sv") return sv_theme_savehelp1(inputs)
	if (locale === "uk") return uk_theme_savehelp1(inputs)
	if (locale === "zh") return zh_theme_savehelp1(inputs)
	if (locale === "es") return es_theme_savehelp1(inputs)
	if (locale === "hi") return hi_theme_savehelp1(inputs)
	if (locale === "pt") return pt_theme_savehelp1(inputs)
	if (locale === "bn") return bn_theme_savehelp1(inputs)
	if (locale === "ru") return ru_theme_savehelp1(inputs)
	if (locale === "ja") return ja_theme_savehelp1(inputs)
	if (locale === "pa") return pa_theme_savehelp1(inputs)
	if (locale === "vi") return vi_theme_savehelp1(inputs)
	if (locale === "yue") return yue_theme_savehelp1(inputs)
	if (locale === "tr") return tr_theme_savehelp1(inputs)
	if (locale === "ar") return ar_theme_savehelp1(inputs)
	if (locale === "wuu") return wuu_theme_savehelp1(inputs)
	if (locale === "mr") return mr_theme_savehelp1(inputs)
	if (locale === "nb") return nb_theme_savehelp1(inputs)
	if (locale === "fi") return fi_theme_savehelp1(inputs)
	if (locale === "da") return da_theme_savehelp1(inputs)
	if (locale === "et") return et_theme_savehelp1(inputs)
	if (locale === "lv") return lv_theme_savehelp1(inputs)
	if (locale === "lt") return lt_theme_savehelp1(inputs)
	if (locale === "pl") return pl_theme_savehelp1(inputs)
	if (locale === "de") return de_theme_savehelp1(inputs)
	if (locale === "nl") return nl_theme_savehelp1(inputs)
	if (locale === "fr") return fr_theme_savehelp1(inputs)
	if (locale === "it") return it_theme_savehelp1(inputs)
	if (locale === "hu") return hu_theme_savehelp1(inputs)
	if (locale === "cs") return cs_theme_savehelp1(inputs)
	if (locale === "ro") return ro_theme_savehelp1(inputs)
	if (locale === "el") return el_theme_savehelp1(inputs)
	if (locale === "bg") return bg_theme_savehelp1(inputs)
	if (locale === "hr") return hr_theme_savehelp1(inputs)
	if (locale === "sr") return sr_theme_savehelp1(inputs)
	if (locale === "sk") return sk_theme_savehelp1(inputs)
	return sl_theme_savehelp1(inputs)
});
export { theme_savehelp1 as "theme_saveHelp" }