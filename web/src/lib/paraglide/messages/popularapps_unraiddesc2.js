/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Popularapps_Unraiddesc2Inputs */

const en_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const sv_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const uk_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const zh_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const es_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const hi_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const pt_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const bn_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const ru_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const ja_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const pa_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const vi_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const yue_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const tr_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const ar_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const wuu_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const mr_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const nb_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const fi_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const da_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const et_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const lv_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const lt_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const pl_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const de_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const nl_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const fr_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const it_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const hu_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const cs_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const ro_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const el_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const bg_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const hr_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const sr_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const sk_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

const sl_popularapps_unraiddesc2 = /** @type {(inputs: Popularapps_Unraiddesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`NAS & server OS`)
};

/**
* | output |
* | --- |
* | "NAS & server OS" |
*
* @param {Popularapps_Unraiddesc2Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const popularapps_unraiddesc2 = /** @type {((inputs?: Popularapps_Unraiddesc2Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Popularapps_Unraiddesc2Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_popularapps_unraiddesc2(inputs)
	if (locale === "sv") return sv_popularapps_unraiddesc2(inputs)
	if (locale === "uk") return uk_popularapps_unraiddesc2(inputs)
	if (locale === "zh") return zh_popularapps_unraiddesc2(inputs)
	if (locale === "es") return es_popularapps_unraiddesc2(inputs)
	if (locale === "hi") return hi_popularapps_unraiddesc2(inputs)
	if (locale === "pt") return pt_popularapps_unraiddesc2(inputs)
	if (locale === "bn") return bn_popularapps_unraiddesc2(inputs)
	if (locale === "ru") return ru_popularapps_unraiddesc2(inputs)
	if (locale === "ja") return ja_popularapps_unraiddesc2(inputs)
	if (locale === "pa") return pa_popularapps_unraiddesc2(inputs)
	if (locale === "vi") return vi_popularapps_unraiddesc2(inputs)
	if (locale === "yue") return yue_popularapps_unraiddesc2(inputs)
	if (locale === "tr") return tr_popularapps_unraiddesc2(inputs)
	if (locale === "ar") return ar_popularapps_unraiddesc2(inputs)
	if (locale === "wuu") return wuu_popularapps_unraiddesc2(inputs)
	if (locale === "mr") return mr_popularapps_unraiddesc2(inputs)
	if (locale === "nb") return nb_popularapps_unraiddesc2(inputs)
	if (locale === "fi") return fi_popularapps_unraiddesc2(inputs)
	if (locale === "da") return da_popularapps_unraiddesc2(inputs)
	if (locale === "et") return et_popularapps_unraiddesc2(inputs)
	if (locale === "lv") return lv_popularapps_unraiddesc2(inputs)
	if (locale === "lt") return lt_popularapps_unraiddesc2(inputs)
	if (locale === "pl") return pl_popularapps_unraiddesc2(inputs)
	if (locale === "de") return de_popularapps_unraiddesc2(inputs)
	if (locale === "nl") return nl_popularapps_unraiddesc2(inputs)
	if (locale === "fr") return fr_popularapps_unraiddesc2(inputs)
	if (locale === "it") return it_popularapps_unraiddesc2(inputs)
	if (locale === "hu") return hu_popularapps_unraiddesc2(inputs)
	if (locale === "cs") return cs_popularapps_unraiddesc2(inputs)
	if (locale === "ro") return ro_popularapps_unraiddesc2(inputs)
	if (locale === "el") return el_popularapps_unraiddesc2(inputs)
	if (locale === "bg") return bg_popularapps_unraiddesc2(inputs)
	if (locale === "hr") return hr_popularapps_unraiddesc2(inputs)
	if (locale === "sr") return sr_popularapps_unraiddesc2(inputs)
	if (locale === "sk") return sk_popularapps_unraiddesc2(inputs)
	return sl_popularapps_unraiddesc2(inputs)
});
export { popularapps_unraiddesc2 as "popularApps_unraidDesc" }