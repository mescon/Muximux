/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Helpinverticoncolors4Inputs */

const en_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const sv_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const uk_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const zh_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const es_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const hi_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const pt_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const bn_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const ru_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const ja_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const pa_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const vi_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const yue_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const tr_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const ar_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const wuu_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const mr_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const nb_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const fi_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const da_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const et_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const lv_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const lt_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const pl_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const de_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const nl_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const fr_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const it_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const hu_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const cs_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const ro_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const el_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const bg_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const hr_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const sr_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const sk_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

const sl_appform_helpinverticoncolors4 = /** @type {(inputs: Appform_Helpinverticoncolors4Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds.`)
};

/**
* | output |
* | --- |
* | "Inverts the icon's colors (black becomes white, white becomes black). Useful for dark icons that are hard to see on dark backgrounds." |
*
* @param {Appform_Helpinverticoncolors4Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const appform_helpinverticoncolors4 = /** @type {((inputs?: Appform_Helpinverticoncolors4Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Helpinverticoncolors4Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_appform_helpinverticoncolors4(inputs)
	if (locale === "sv") return sv_appform_helpinverticoncolors4(inputs)
	if (locale === "uk") return uk_appform_helpinverticoncolors4(inputs)
	if (locale === "zh") return zh_appform_helpinverticoncolors4(inputs)
	if (locale === "es") return es_appform_helpinverticoncolors4(inputs)
	if (locale === "hi") return hi_appform_helpinverticoncolors4(inputs)
	if (locale === "pt") return pt_appform_helpinverticoncolors4(inputs)
	if (locale === "bn") return bn_appform_helpinverticoncolors4(inputs)
	if (locale === "ru") return ru_appform_helpinverticoncolors4(inputs)
	if (locale === "ja") return ja_appform_helpinverticoncolors4(inputs)
	if (locale === "pa") return pa_appform_helpinverticoncolors4(inputs)
	if (locale === "vi") return vi_appform_helpinverticoncolors4(inputs)
	if (locale === "yue") return yue_appform_helpinverticoncolors4(inputs)
	if (locale === "tr") return tr_appform_helpinverticoncolors4(inputs)
	if (locale === "ar") return ar_appform_helpinverticoncolors4(inputs)
	if (locale === "wuu") return wuu_appform_helpinverticoncolors4(inputs)
	if (locale === "mr") return mr_appform_helpinverticoncolors4(inputs)
	if (locale === "nb") return nb_appform_helpinverticoncolors4(inputs)
	if (locale === "fi") return fi_appform_helpinverticoncolors4(inputs)
	if (locale === "da") return da_appform_helpinverticoncolors4(inputs)
	if (locale === "et") return et_appform_helpinverticoncolors4(inputs)
	if (locale === "lv") return lv_appform_helpinverticoncolors4(inputs)
	if (locale === "lt") return lt_appform_helpinverticoncolors4(inputs)
	if (locale === "pl") return pl_appform_helpinverticoncolors4(inputs)
	if (locale === "de") return de_appform_helpinverticoncolors4(inputs)
	if (locale === "nl") return nl_appform_helpinverticoncolors4(inputs)
	if (locale === "fr") return fr_appform_helpinverticoncolors4(inputs)
	if (locale === "it") return it_appform_helpinverticoncolors4(inputs)
	if (locale === "hu") return hu_appform_helpinverticoncolors4(inputs)
	if (locale === "cs") return cs_appform_helpinverticoncolors4(inputs)
	if (locale === "ro") return ro_appform_helpinverticoncolors4(inputs)
	if (locale === "el") return el_appform_helpinverticoncolors4(inputs)
	if (locale === "bg") return bg_appform_helpinverticoncolors4(inputs)
	if (locale === "hr") return hr_appform_helpinverticoncolors4(inputs)
	if (locale === "sr") return sr_appform_helpinverticoncolors4(inputs)
	if (locale === "sk") return sk_appform_helpinverticoncolors4(inputs)
	return sl_appform_helpinverticoncolors4(inputs)
});
export { appform_helpinverticoncolors4 as "appForm_helpInvertIconColors" }