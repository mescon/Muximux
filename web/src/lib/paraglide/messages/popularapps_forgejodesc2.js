/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Popularapps_Forgejodesc2Inputs */

const en_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Self-hosted Git (Gitea fork)`)
};

const sv_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Självhostad Git (Gitea-fork)`)
};

const uk_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Самостійно розгорнутий Git (форк Gitea)`)
};

const zh_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`自托管 Git（Gitea 分支）`)
};

const es_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Git autoalojado (fork de Gitea)`)
};

const hi_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`स्व-होस्टेड Git (Gitea फोर्क)`)
};

const pt_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Git auto-alojado (fork do Gitea)`)
};

const bn_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`সেলফ-হোস্টেড Git (Gitea ফর্ক)`)
};

const ru_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Git-сервер (форк Gitea)`)
};

const ja_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`セルフホスト型 Git（Gitea のフォーク）`)
};

const vi_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Git tự lưu trữ (nhánh của Gitea)`)
};

const yue_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`自托管 Git（Gitea 分支）`)
};

const tr_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Kendi sunucunuzda Git (Gitea çatallaması)`)
};

const ar_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Git مُستضاف ذاتيًا (انشقاق عن Gitea)`)
};

const wuu_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`自托管 Git（Gitea 分支）`)
};

const mr_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`स्व-होस्टेड Git (Gitea फोर्क)`)
};

const nb_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Selvhostet Git (Gitea-fork)`)
};

const fi_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Itse isännöity Git (Gitea-haaroitus)`)
};

const da_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Selvhostet Git (Gitea-fork)`)
};

const et_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Ise hallatav Git (Gitea haru)`)
};

const lv_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pašmitināts Git (Gitea atzarojums)`)
};

const lt_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Savarankiškai talpinamas Git (Gitea atšaka)`)
};

const pl_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Samodzielnie hostowany Git (fork Gitea)`)
};

const de_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Selbstgehostetes Git (Gitea-Fork)`)
};

const nl_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zelf-gehoste Git (Gitea-fork)`)
};

const fr_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Git auto-hébergé (fork de Gitea)`)
};

const it_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Git self-hosted (fork di Gitea)`)
};

const hu_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Saját üzemeltetésű Git (Gitea fork)`)
};

const cs_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Vlastně hostovaný Git (fork Gitea)`)
};

const ro_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Git auto-găzduit (fork al Gitea)`)
};

const el_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Αυτοφιλοξενούμενο Git (fork του Gitea)`)
};

const bg_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Самостоятелно хостван Git (форк на Gitea)`)
};

const hr_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Samostalno hostani Git (Gitea fork)`)
};

const sr_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Samostalno hostovani Git (Gitea fork)`)
};

const sk_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Vlastne hostovaný Git (fork Gitea)`)
};

const sl_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Samostojno gostovan Git (razcep Gitea)`)
};

const ko_popularapps_forgejodesc2 = /** @type {(inputs: Popularapps_Forgejodesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`자체 호스팅 Git (Gitea 포크)`)
};

/**
* | output |
* | --- |
* | "Self-hosted Git (Gitea fork)" |
*
* @param {Popularapps_Forgejodesc2Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
const popularapps_forgejodesc2 = /** @type {((inputs?: Popularapps_Forgejodesc2Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Popularapps_Forgejodesc2Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_popularapps_forgejodesc2(inputs)
	if (locale === "sv") return sv_popularapps_forgejodesc2(inputs)
	if (locale === "uk") return uk_popularapps_forgejodesc2(inputs)
	if (locale === "zh") return zh_popularapps_forgejodesc2(inputs)
	if (locale === "es") return es_popularapps_forgejodesc2(inputs)
	if (locale === "hi") return hi_popularapps_forgejodesc2(inputs)
	if (locale === "pt") return pt_popularapps_forgejodesc2(inputs)
	if (locale === "bn") return bn_popularapps_forgejodesc2(inputs)
	if (locale === "ru") return ru_popularapps_forgejodesc2(inputs)
	if (locale === "ja") return ja_popularapps_forgejodesc2(inputs)
	if (locale === "vi") return vi_popularapps_forgejodesc2(inputs)
	if (locale === "yue") return yue_popularapps_forgejodesc2(inputs)
	if (locale === "tr") return tr_popularapps_forgejodesc2(inputs)
	if (locale === "ar") return ar_popularapps_forgejodesc2(inputs)
	if (locale === "wuu") return wuu_popularapps_forgejodesc2(inputs)
	if (locale === "mr") return mr_popularapps_forgejodesc2(inputs)
	if (locale === "nb") return nb_popularapps_forgejodesc2(inputs)
	if (locale === "fi") return fi_popularapps_forgejodesc2(inputs)
	if (locale === "da") return da_popularapps_forgejodesc2(inputs)
	if (locale === "et") return et_popularapps_forgejodesc2(inputs)
	if (locale === "lv") return lv_popularapps_forgejodesc2(inputs)
	if (locale === "lt") return lt_popularapps_forgejodesc2(inputs)
	if (locale === "pl") return pl_popularapps_forgejodesc2(inputs)
	if (locale === "de") return de_popularapps_forgejodesc2(inputs)
	if (locale === "nl") return nl_popularapps_forgejodesc2(inputs)
	if (locale === "fr") return fr_popularapps_forgejodesc2(inputs)
	if (locale === "it") return it_popularapps_forgejodesc2(inputs)
	if (locale === "hu") return hu_popularapps_forgejodesc2(inputs)
	if (locale === "cs") return cs_popularapps_forgejodesc2(inputs)
	if (locale === "ro") return ro_popularapps_forgejodesc2(inputs)
	if (locale === "el") return el_popularapps_forgejodesc2(inputs)
	if (locale === "bg") return bg_popularapps_forgejodesc2(inputs)
	if (locale === "hr") return hr_popularapps_forgejodesc2(inputs)
	if (locale === "sr") return sr_popularapps_forgejodesc2(inputs)
	if (locale === "sk") return sk_popularapps_forgejodesc2(inputs)
	if (locale === "sl") return sl_popularapps_forgejodesc2(inputs)
	return ko_popularapps_forgejodesc2(inputs)
});
export { popularapps_forgejodesc2 as "popularApps_forgejoDesc" }