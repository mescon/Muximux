/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Helpurl2Inputs */

const en_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`The full address of the application. Used to load the app in an iframe, or as the link when opened in a new tab/window.`)
};

const sv_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Applikationens fullständiga adress. Används för att ladda appen i en iframe, eller som länk vid öppning i ny flik/nytt fönster.`)
};

const uk_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Повна адреса застосунку. Використовується для завантаження в iframe або як посилання при відкритті в новій вкладці/вікні.`)
};

const zh_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`应用的完整地址。用于在 iframe 中加载应用，或在新标签页/窗口中打开时作为链接。`)
};

const es_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`La dirección completa de la aplicación. Se usa para cargar la app en un iframe, o como enlace al abrirla en una nueva pestaña/ventana.`)
};

const hi_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`एप्लिकेशन का पूरा पता। ऐप को iframe में लोड करने या नए टैब/विंडो में खोलने पर लिंक के रूप में उपयोग होता है।`)
};

const pt_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`O endereço completo da aplicação. Usado para carregar a app num iframe, ou como ligação ao abrir num novo separador/janela.`)
};

const bn_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`অ্যাপ্লিকেশনের পূর্ণ ঠিকানা। iframe-এ অ্যাপ লোড করতে বা নতুন ট্যাব/উইন্ডোতে লিংক হিসেবে ব্যবহৃত।`)
};

const ru_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Полный адрес приложения. Используется для загрузки в iframe или как ссылка при открытии в новой вкладке/окне.`)
};

const ja_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`アプリケーションの完全なアドレス。iframe でアプリを読み込む場合や、新しいタブ/ウィンドウで開く際のリンクとして使用されます。`)
};

const vi_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Địa chỉ đầy đủ của ứng dụng. Dùng để tải ứng dụng trong iframe hoặc làm liên kết khi mở trong tab/cửa sổ mới.`)
};

const yue_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`應用程式嘅完整地址。用嚟喺 iframe 入面載入應用程式，或者喺新分頁/視窗開啟時作為連結。`)
};

const tr_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Uygulamanın tam adresi. Uygulamayı iframe'de yüklemek veya yeni sekme/pencerede açıldığında bağlantı olarak kullanılır.`)
};

const ar_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`العنوان الكامل للتطبيق. يُستخدم لتحميل التطبيق في iframe أو كرابط عند الفتح في تبويب/نافذة جديدة.`)
};

const wuu_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`应用的完整地址。用来在 iframe 里加载应用，或者在新标签页/新窗口中打开时作为链接。`)
};

const mr_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`अॅप्लिकेशनचा पूर्ण पत्ता. iframe मध्ये अॅप लोड करण्यासाठी, किंवा नवीन टॅब/विंडोमध्ये उघडताना दुवा म्हणून वापरले जाते.`)
};

const nb_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Applikasjonens fullstendige adresse. Brukes til å laste appen i en iframe, eller som lenke ved åpning i ny fane/nytt vindu.`)
};

const fi_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sovelluksen täydellinen osoite. Käytetään sovelluksen lataamiseen iframe-kehyksessä tai linkkinä avattaessa uudessa välilehdessä/ikkunassa.`)
};

const da_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Applikationens fulde adresse. Bruges til at indlæse appen i en iframe, eller som link ved åbning i ny fane/nyt vindue.`)
};

const et_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Rakenduse täielik aadress. Kasutatakse rakenduse laadimiseks iframe-is või lingina uues vahekaardis/aknas avamisel.`)
};

const lv_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lietotnes pilna adrese. Tiek izmantota lietotnes ielādēšanai iframe, vai kā saite atverot jaunā cilnē/logā.`)
};

const lt_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pilnas programos adresas. Naudojamas programai įkelti iframe arba kaip nuoroda atidarant naujoje kortelėje/lange.`)
};

const pl_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pełny adres aplikacji. Używany do ładowania w iframe lub jako link przy otwieraniu w nowej karcie/oknie.`)
};

const de_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Die vollständige Adresse der Anwendung. Wird verwendet, um die App in einem iframe zu laden oder als Link beim Öffnen in einem neuen Tab/Fenster.`)
};

const nl_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Het volledige adres van de applicatie. Wordt gebruikt om de app in een iframe te laden of als link bij openen in een nieuw tabblad/venster.`)
};

const fr_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`L'adresse complète de l'application. Utilisée pour charger l'app dans un iframe ou comme lien lors de l'ouverture dans un nouvel onglet/fenêtre.`)
};

const it_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`L'indirizzo completo dell'applicazione. Usato per caricare l'app in un iframe o come link quando aperta in una nuova scheda/finestra.`)
};

const hu_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Az alkalmazás teljes címe. Az iframe-be töltéshez vagy új lapon/ablakban való megnyitáskor linkként használatos.`)
};

const cs_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Úplná adresa aplikace. Používá se pro načtení v iframe nebo jako odkaz při otevření v nové kartě/okně.`)
};

const ro_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Adresa completă a aplicației. Folosită pentru a încărca aplicația într-un iframe sau ca link când se deschide într-o filă/fereastră nouă.`)
};

const el_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Η πλήρης διεύθυνση της εφαρμογής. Χρησιμοποιείται για φόρτωση της εφαρμογής σε iframe ή ως σύνδεσμος κατά το άνοιγμα σε νέα καρτέλα/παράθυρο.`)
};

const bg_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Пълният адрес на приложението. Използва се за зареждане на приложението в iframe или като връзка при отваряне в нов раздел/прозорец.`)
};

const hr_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Puna adresa aplikacije. Koristi se za učitavanje aplikacije u iframe ili kao poveznica pri otvaranju u novoj kartici/prozoru.`)
};

const sr_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Puna adresa aplikacije. Koristi se za učitavanje aplikacije u iframe ili kao link pri otvaranju u novoj kartici/prozoru.`)
};

const sk_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Úplná adresa aplikácie. Používa sa na načítanie v iframe alebo ako odkaz pri otvorení v novej karte/okne.`)
};

const sl_appform_helpurl2 = /** @type {(inputs: Appform_Helpurl2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Polni naslov aplikacije. Uporablja se za nalaganje aplikacije v iframe ali kot povezava pri odpiranju v novem zavihku/oknu.`)
};

/**
* | output |
* | --- |
* | "The full address of the application. Used to load the app in an iframe, or as the link when opened in a new tab/window." |
*
* @param {Appform_Helpurl2Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const appform_helpurl2 = /** @type {((inputs?: Appform_Helpurl2Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Helpurl2Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_appform_helpurl2(inputs)
	if (locale === "sv") return sv_appform_helpurl2(inputs)
	if (locale === "uk") return uk_appform_helpurl2(inputs)
	if (locale === "zh") return zh_appform_helpurl2(inputs)
	if (locale === "es") return es_appform_helpurl2(inputs)
	if (locale === "hi") return hi_appform_helpurl2(inputs)
	if (locale === "pt") return pt_appform_helpurl2(inputs)
	if (locale === "bn") return bn_appform_helpurl2(inputs)
	if (locale === "ru") return ru_appform_helpurl2(inputs)
	if (locale === "ja") return ja_appform_helpurl2(inputs)
	if (locale === "vi") return vi_appform_helpurl2(inputs)
	if (locale === "yue") return yue_appform_helpurl2(inputs)
	if (locale === "tr") return tr_appform_helpurl2(inputs)
	if (locale === "ar") return ar_appform_helpurl2(inputs)
	if (locale === "wuu") return wuu_appform_helpurl2(inputs)
	if (locale === "mr") return mr_appform_helpurl2(inputs)
	if (locale === "nb") return nb_appform_helpurl2(inputs)
	if (locale === "fi") return fi_appform_helpurl2(inputs)
	if (locale === "da") return da_appform_helpurl2(inputs)
	if (locale === "et") return et_appform_helpurl2(inputs)
	if (locale === "lv") return lv_appform_helpurl2(inputs)
	if (locale === "lt") return lt_appform_helpurl2(inputs)
	if (locale === "pl") return pl_appform_helpurl2(inputs)
	if (locale === "de") return de_appform_helpurl2(inputs)
	if (locale === "nl") return nl_appform_helpurl2(inputs)
	if (locale === "fr") return fr_appform_helpurl2(inputs)
	if (locale === "it") return it_appform_helpurl2(inputs)
	if (locale === "hu") return hu_appform_helpurl2(inputs)
	if (locale === "cs") return cs_appform_helpurl2(inputs)
	if (locale === "ro") return ro_appform_helpurl2(inputs)
	if (locale === "el") return el_appform_helpurl2(inputs)
	if (locale === "bg") return bg_appform_helpurl2(inputs)
	if (locale === "hr") return hr_appform_helpurl2(inputs)
	if (locale === "sr") return sr_appform_helpurl2(inputs)
	if (locale === "sk") return sk_appform_helpurl2(inputs)
	return sl_appform_helpurl2(inputs)
});
export { appform_helpurl2 as "appForm_helpUrl" }