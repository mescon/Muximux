/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Toast_Configimported1Inputs */

const en_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Configuration imported - save to apply changes`)
};

const sv_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfigurationen importerad – spara för att tillämpa ändringar`)
};

const uk_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Конфігурацію імпортовано – збережіть, щоб застосувати зміни`)
};

const zh_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`配置已导入 - 保存以应用更改`)
};

const es_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Configuración importada — guarda para aplicar los cambios`)
};

const hi_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`कॉन्फ़िगरेशन आयात किया गया — परिवर्तन लागू करने के लिए सहेजें`)
};

const pt_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Configuração importada — guarda para aplicar as alterações`)
};

const bn_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`কনফিগারেশন আমদানি হয়েছে — পরিবর্তন প্রয়োগ করতে সংরক্ষণ করুন`)
};

const ru_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Конфигурация импортирована — сохраните для применения изменений`)
};

const ja_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`設定をインポートしました - 保存して変更を適用してください`)
};

const vi_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Đã nhập cấu hình - lưu để áp dụng thay đổi`)
};

const yue_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`設定已匯入 - 儲存以套用變更`)
};

const tr_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Yapılandırma içe aktarıldı — değişiklikleri uygulamak için kaydedin`)
};

const ar_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`تم استيراد الإعدادات — احفظ لتطبيق التغييرات`)
};

const wuu_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`配置导入了——保存后生效`)
};

const mr_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`कॉन्फिगरेशन आयात झाले — बदल लागू करण्यासाठी जतन करा`)
};

const nb_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfigurasjon importert – lagre for å ta i bruk endringer`)
};

const fi_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfiguraatio tuotu – tallenna ottaaksesi muutokset käyttöön`)
};

const da_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfiguration importeret – gem for at anvende ændringer`)
};

const et_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfiguratsioon imporditud – salvestage muudatuste rakendamiseks`)
};

const lv_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfigurācija importēta – saglabājiet, lai piemērotu izmaiņas`)
};

const lt_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfigūracija importuota – išsaugokite, kad pritaikytumėte pakeitimus`)
};

const pl_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfiguracja zaimportowana — zapisz, aby zastosować zmiany`)
};

const de_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfiguration importiert – speichern, um Änderungen zu übernehmen`)
};

const nl_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Configuratie geïmporteerd – sla op om wijzigingen toe te passen`)
};

const fr_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Configuration importée – enregistre pour appliquer les modifications`)
};

const it_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Configurazione importata – salva per applicare le modifiche`)
};

const hu_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfiguráció importálva — mentse a változtatások alkalmazásához`)
};

const cs_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfigurace importována — uložte pro aplikování změn`)
};

const ro_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Configurația a fost importată - salvează pentru a aplica modificările`)
};

const el_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Η διαμόρφωση εισήχθη - αποθηκεύστε για εφαρμογή αλλαγών`)
};

const bg_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Конфигурацията е импортирана - запази за прилагане на промените`)
};

const hr_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfiguracija uvezena — spremite za primjenu promjena`)
};

const sr_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfiguracija uvezena — sačuvajte da primenite promene`)
};

const sk_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfigurácia importovaná — uložte pre aplikovanie zmien`)
};

const sl_toast_configimported1 = /** @type {(inputs: Toast_Configimported1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Konfiguracija uvožena — shranite za uveljavitev sprememb`)
};

/**
* | output |
* | --- |
* | "Configuration imported - save to apply changes" |
*
* @param {Toast_Configimported1Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const toast_configimported1 = /** @type {((inputs?: Toast_Configimported1Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Toast_Configimported1Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_toast_configimported1(inputs)
	if (locale === "sv") return sv_toast_configimported1(inputs)
	if (locale === "uk") return uk_toast_configimported1(inputs)
	if (locale === "zh") return zh_toast_configimported1(inputs)
	if (locale === "es") return es_toast_configimported1(inputs)
	if (locale === "hi") return hi_toast_configimported1(inputs)
	if (locale === "pt") return pt_toast_configimported1(inputs)
	if (locale === "bn") return bn_toast_configimported1(inputs)
	if (locale === "ru") return ru_toast_configimported1(inputs)
	if (locale === "ja") return ja_toast_configimported1(inputs)
	if (locale === "vi") return vi_toast_configimported1(inputs)
	if (locale === "yue") return yue_toast_configimported1(inputs)
	if (locale === "tr") return tr_toast_configimported1(inputs)
	if (locale === "ar") return ar_toast_configimported1(inputs)
	if (locale === "wuu") return wuu_toast_configimported1(inputs)
	if (locale === "mr") return mr_toast_configimported1(inputs)
	if (locale === "nb") return nb_toast_configimported1(inputs)
	if (locale === "fi") return fi_toast_configimported1(inputs)
	if (locale === "da") return da_toast_configimported1(inputs)
	if (locale === "et") return et_toast_configimported1(inputs)
	if (locale === "lv") return lv_toast_configimported1(inputs)
	if (locale === "lt") return lt_toast_configimported1(inputs)
	if (locale === "pl") return pl_toast_configimported1(inputs)
	if (locale === "de") return de_toast_configimported1(inputs)
	if (locale === "nl") return nl_toast_configimported1(inputs)
	if (locale === "fr") return fr_toast_configimported1(inputs)
	if (locale === "it") return it_toast_configimported1(inputs)
	if (locale === "hu") return hu_toast_configimported1(inputs)
	if (locale === "cs") return cs_toast_configimported1(inputs)
	if (locale === "ro") return ro_toast_configimported1(inputs)
	if (locale === "el") return el_toast_configimported1(inputs)
	if (locale === "bg") return bg_toast_configimported1(inputs)
	if (locale === "hr") return hr_toast_configimported1(inputs)
	if (locale === "sr") return sr_toast_configimported1(inputs)
	if (locale === "sk") return sk_toast_configimported1(inputs)
	return sl_toast_configimported1(inputs)
});
export { toast_configimported1 as "toast_configImported" }