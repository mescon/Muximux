/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Login_SsoInputs */

const en_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const sv_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const uk_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const zh_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const es_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const hi_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const pt_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const bn_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const ru_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const ja_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const pa_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const vi_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const yue_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const tr_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const ar_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const wuu_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const mr_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const nb_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const fi_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const da_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const et_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const lv_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const lt_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const pl_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const de_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const nl_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const fr_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const it_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const hu_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const cs_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const ro_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const el_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const bg_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const hr_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const sr_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const sk_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

const sl_login_sso = /** @type {(inputs: Login_SsoInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Sign in with SSO`)
};

/**
* | output |
* | --- |
* | "Sign in with SSO" |
*
* @param {Login_SsoInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
export const login_sso = /** @type {((inputs?: Login_SsoInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Login_SsoInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_login_sso(inputs)
	if (locale === "sv") return sv_login_sso(inputs)
	if (locale === "uk") return uk_login_sso(inputs)
	if (locale === "zh") return zh_login_sso(inputs)
	if (locale === "es") return es_login_sso(inputs)
	if (locale === "hi") return hi_login_sso(inputs)
	if (locale === "pt") return pt_login_sso(inputs)
	if (locale === "bn") return bn_login_sso(inputs)
	if (locale === "ru") return ru_login_sso(inputs)
	if (locale === "ja") return ja_login_sso(inputs)
	if (locale === "pa") return pa_login_sso(inputs)
	if (locale === "vi") return vi_login_sso(inputs)
	if (locale === "yue") return yue_login_sso(inputs)
	if (locale === "tr") return tr_login_sso(inputs)
	if (locale === "ar") return ar_login_sso(inputs)
	if (locale === "wuu") return wuu_login_sso(inputs)
	if (locale === "mr") return mr_login_sso(inputs)
	if (locale === "nb") return nb_login_sso(inputs)
	if (locale === "fi") return fi_login_sso(inputs)
	if (locale === "da") return da_login_sso(inputs)
	if (locale === "et") return et_login_sso(inputs)
	if (locale === "lv") return lv_login_sso(inputs)
	if (locale === "lt") return lt_login_sso(inputs)
	if (locale === "pl") return pl_login_sso(inputs)
	if (locale === "de") return de_login_sso(inputs)
	if (locale === "nl") return nl_login_sso(inputs)
	if (locale === "fr") return fr_login_sso(inputs)
	if (locale === "it") return it_login_sso(inputs)
	if (locale === "hu") return hu_login_sso(inputs)
	if (locale === "cs") return cs_login_sso(inputs)
	if (locale === "ro") return ro_login_sso(inputs)
	if (locale === "el") return el_login_sso(inputs)
	if (locale === "bg") return bg_login_sso(inputs)
	if (locale === "hr") return hr_login_sso(inputs)
	if (locale === "sr") return sr_login_sso(inputs)
	if (locale === "sk") return sk_login_sso(inputs)
	return sl_login_sso(inputs)
});