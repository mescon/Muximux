/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Helpskiptls3Inputs */

const en_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Enabled by default. Disable this only if the backend has a valid, trusted TLS certificate and you want strict verification.`)
};

const sv_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Aktiverat som standard. Inaktivera detta bara om backend-servern har ett giltigt, betrott TLS-certifikat och du vill ha strikt verifiering.`)
};

const uk_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Увімкнено за замовчуванням. Вимкніть лише якщо backend має дійсний, довірений сертифікат TLS і ви бажаєте суворої перевірки.`)
};

const zh_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`默认启用。仅在后端拥有有效、受信任的 TLS 证书且你需要严格验证时才禁用此选项。`)
};

const es_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Activado por defecto. Desactívalo solo si el backend tiene un certificado TLS válido y de confianza y quieres verificación estricta.`)
};

const hi_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`डिफ़ॉल्ट रूप से सक्षम। केवल तभी अक्षम करें जब बैकएंड के पास वैध, विश्वसनीय TLS प्रमाणपत्र हो और आप सख्त सत्यापन चाहते हों।`)
};

const pt_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Ativado por predefinição. Desativa apenas se o backend tiver um certificado TLS válido e de confiança e quiseres verificação rigorosa.`)
};

const bn_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`ডিফল্টভাবে সক্রিয়। শুধুমাত্র ব্যাকএন্ডে বৈধ, বিশ্বস্ত TLS সার্টিফিকেট থাকলে এবং কঠোর যাচাই চাইলে নিষ্ক্রিয় করুন।`)
};

const ru_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Включено по умолчанию. Отключайте только если сервер имеет действительный доверенный TLS-сертификат и вы хотите строгую проверку.`)
};

const ja_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`デフォルトで有効です。バックエンドが有効で信頼された TLS 証明書を持ち、厳密な検証が必要な場合のみ無効にしてください。`)
};

const vi_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Được bật theo mặc định. Chỉ tắt nếu backend có chứng chỉ TLS hợp lệ, đáng tin cậy và bạn muốn xác minh nghiêm ngặt.`)
};

const yue_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`預設啟用。只有喺後端有有效、受信任嘅 TLS 憑證而你需要嚴格驗證時先至停用。`)
};

const tr_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Varsayılan olarak etkindir. Yalnızca arka ucun geçerli, güvenilir bir TLS sertifikası varsa ve sıkı doğrulama istiyorsanız devre dışı bırakın.`)
};

const ar_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`مفعّل افتراضيًا. عطّله فقط إذا كان الخادم لديه شهادة TLS صالحة وموثوقة وتريد التحقق الصارم.`)
};

const wuu_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`默认启用。只有后端有合法受信任的 TLS 证书并且你要严格验证时才关闭。`)
};

const mr_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`डीफॉल्टनुसार सक्षम. बॅकएंडकडे वैध, विश्वसनीय TLS प्रमाणपत्र असेल आणि तुम्हाला कडक सत्यापन हवे असेल तरच अक्षम करा.`)
};

const nb_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Aktivert som standard. Deaktiver dette bare hvis backend-serveren har et gyldig, betrodd TLS-sertifikat og du ønsker streng verifisering.`)
};

const fi_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Käytössä oletuksena. Poista käytöstä vain, jos taustajärjestelmällä on voimassa oleva, luotettu TLS-varmenne ja haluat tiukan varmennuksen.`)
};

const da_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Aktiveret som standard. Deaktivér dette kun hvis backend-serveren har et gyldigt, betroet TLS-certifikat og du ønsker streng verifikation.`)
};

const et_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Vaikimisi lubatud. Keelake see ainult juhul, kui tagarakendusel on kehtiv, usaldatud TLS sertifikaat ja soovite ranget verifitseerimist.`)
};

const lv_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Iespējots pēc noklusējuma. Atspējojiet tikai tad, ja aizmugursistēmai ir derīgs, uzticams TLS sertifikāts un vēlaties stingru verifikāciju.`)
};

const lt_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Įjungta pagal numatytuosius nustatymus. Išjunkite tik jei backend turi galiojantį, patikimą TLS sertifikatą ir norite griežto tikrinimo.`)
};

const pl_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Domyślnie włączone. Wyłącz tylko jeśli backend ma ważny, zaufany certyfikat TLS i chcesz ścisłej weryfikacji.`)
};

const de_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Standardmäßig aktiviert. Deaktivieren Sie dies nur, wenn das Backend ein gültiges, vertrauenswürdiges TLS-Zertifikat hat und Sie eine strikte Überprüfung wünschen.`)
};

const nl_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Standaard ingeschakeld. Schakel dit alleen uit als de backend een geldig, vertrouwd TLS-certificaat heeft en je strikte verificatie wilt.`)
};

const fr_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Activé par défaut. Désactive uniquement si le backend dispose d'un certificat TLS valide et de confiance et que tu souhaites une vérification stricte.`)
};

const it_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Attivato per impostazione predefinita. Disabilita solo se il backend ha un certificato TLS valido e attendibile e vuoi una verifica rigorosa.`)
};

const hu_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Alapértelmezetten engedélyezve. Csak akkor tiltsa le, ha a backend érvényes, megbízható TLS tanúsítvánnyal rendelkezik és szigorú ellenőrzést szeretne.`)
};

const cs_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Ve výchozím nastavení povoleno. Zakažte pouze pokud má backend platný, důvěryhodný certifikát TLS a chcete přísné ověření.`)
};

const ro_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Activat implicit. Dezactivează doar dacă backend-ul are un certificat TLS valid și de încredere și dorești verificare strictă.`)
};

const el_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Ενεργοποιημένο εξ ορισμού. Απενεργοποιήστε μόνο αν το backend έχει έγκυρο, αξιόπιστο πιστοποιητικό TLS και θέλετε αυστηρή επαλήθευση.`)
};

const bg_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Активирано по подразбиране. Деактивирай само ако бекендът има валиден, доверен TLS сертификат и искаш строга проверка.`)
};

const hr_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Omogućeno prema zadanim postavkama. Onemogućite ovo samo ako pozadinski sustav ima valjan, pouzdan TLS certifikat i želite strogu provjeru.`)
};

const sr_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Omogućeno prema podrazumevanim podešavanjima. Onemogućite ovo samo ako pozadinski sistem ima valjan, pouzdan TLS sertifikat i želite strogu proveru.`)
};

const sk_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Predvolene povolené. Zakážte iba ak má backend platný, dôveryhodný certifikát TLS a chcete prísne overenie.`)
};

const sl_appform_helpskiptls3 = /** @type {(inputs: Appform_Helpskiptls3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Privzeto omogočeno. Onemogočite to le, če ima zaledni sistem veljaven, zaupanja vreden TLS certifikat in želite strogo preverjanje.`)
};

/**
* | output |
* | --- |
* | "Enabled by default. Disable this only if the backend has a valid, trusted TLS certificate and you want strict verification." |
*
* @param {Appform_Helpskiptls3Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const appform_helpskiptls3 = /** @type {((inputs?: Appform_Helpskiptls3Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Helpskiptls3Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_appform_helpskiptls3(inputs)
	if (locale === "sv") return sv_appform_helpskiptls3(inputs)
	if (locale === "uk") return uk_appform_helpskiptls3(inputs)
	if (locale === "zh") return zh_appform_helpskiptls3(inputs)
	if (locale === "es") return es_appform_helpskiptls3(inputs)
	if (locale === "hi") return hi_appform_helpskiptls3(inputs)
	if (locale === "pt") return pt_appform_helpskiptls3(inputs)
	if (locale === "bn") return bn_appform_helpskiptls3(inputs)
	if (locale === "ru") return ru_appform_helpskiptls3(inputs)
	if (locale === "ja") return ja_appform_helpskiptls3(inputs)
	if (locale === "vi") return vi_appform_helpskiptls3(inputs)
	if (locale === "yue") return yue_appform_helpskiptls3(inputs)
	if (locale === "tr") return tr_appform_helpskiptls3(inputs)
	if (locale === "ar") return ar_appform_helpskiptls3(inputs)
	if (locale === "wuu") return wuu_appform_helpskiptls3(inputs)
	if (locale === "mr") return mr_appform_helpskiptls3(inputs)
	if (locale === "nb") return nb_appform_helpskiptls3(inputs)
	if (locale === "fi") return fi_appform_helpskiptls3(inputs)
	if (locale === "da") return da_appform_helpskiptls3(inputs)
	if (locale === "et") return et_appform_helpskiptls3(inputs)
	if (locale === "lv") return lv_appform_helpskiptls3(inputs)
	if (locale === "lt") return lt_appform_helpskiptls3(inputs)
	if (locale === "pl") return pl_appform_helpskiptls3(inputs)
	if (locale === "de") return de_appform_helpskiptls3(inputs)
	if (locale === "nl") return nl_appform_helpskiptls3(inputs)
	if (locale === "fr") return fr_appform_helpskiptls3(inputs)
	if (locale === "it") return it_appform_helpskiptls3(inputs)
	if (locale === "hu") return hu_appform_helpskiptls3(inputs)
	if (locale === "cs") return cs_appform_helpskiptls3(inputs)
	if (locale === "ro") return ro_appform_helpskiptls3(inputs)
	if (locale === "el") return el_appform_helpskiptls3(inputs)
	if (locale === "bg") return bg_appform_helpskiptls3(inputs)
	if (locale === "hr") return hr_appform_helpskiptls3(inputs)
	if (locale === "sr") return sr_appform_helpskiptls3(inputs)
	if (locale === "sk") return sk_appform_helpskiptls3(inputs)
	return sl_appform_helpskiptls3(inputs)
});
export { appform_helpskiptls3 as "appForm_helpSkipTls" }