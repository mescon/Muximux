/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Proxydesc2Inputs */

const en_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const sv_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const uk_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const zh_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const es_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const hi_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const pt_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const bn_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const ru_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const ja_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const pa_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const vi_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const yue_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const tr_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const ar_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const wuu_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const mr_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const nb_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const fi_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const da_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const et_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const lv_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const lt_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const pl_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const de_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const nl_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const fr_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const it_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const hu_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const cs_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const ro_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const el_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const bg_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const hr_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const sr_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const sk_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

const sl_appform_proxydesc2 = /** @type {(inputs: Appform_Proxydesc2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Route traffic through the built-in proxy to avoid CORS and mixed-content issues`)
};

/**
* | output |
* | --- |
* | "Route traffic through the built-in proxy to avoid CORS and mixed-content issues" |
*
* @param {Appform_Proxydesc2Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const appform_proxydesc2 = /** @type {((inputs?: Appform_Proxydesc2Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Proxydesc2Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_appform_proxydesc2(inputs)
	if (locale === "sv") return sv_appform_proxydesc2(inputs)
	if (locale === "uk") return uk_appform_proxydesc2(inputs)
	if (locale === "zh") return zh_appform_proxydesc2(inputs)
	if (locale === "es") return es_appform_proxydesc2(inputs)
	if (locale === "hi") return hi_appform_proxydesc2(inputs)
	if (locale === "pt") return pt_appform_proxydesc2(inputs)
	if (locale === "bn") return bn_appform_proxydesc2(inputs)
	if (locale === "ru") return ru_appform_proxydesc2(inputs)
	if (locale === "ja") return ja_appform_proxydesc2(inputs)
	if (locale === "pa") return pa_appform_proxydesc2(inputs)
	if (locale === "vi") return vi_appform_proxydesc2(inputs)
	if (locale === "yue") return yue_appform_proxydesc2(inputs)
	if (locale === "tr") return tr_appform_proxydesc2(inputs)
	if (locale === "ar") return ar_appform_proxydesc2(inputs)
	if (locale === "wuu") return wuu_appform_proxydesc2(inputs)
	if (locale === "mr") return mr_appform_proxydesc2(inputs)
	if (locale === "nb") return nb_appform_proxydesc2(inputs)
	if (locale === "fi") return fi_appform_proxydesc2(inputs)
	if (locale === "da") return da_appform_proxydesc2(inputs)
	if (locale === "et") return et_appform_proxydesc2(inputs)
	if (locale === "lv") return lv_appform_proxydesc2(inputs)
	if (locale === "lt") return lt_appform_proxydesc2(inputs)
	if (locale === "pl") return pl_appform_proxydesc2(inputs)
	if (locale === "de") return de_appform_proxydesc2(inputs)
	if (locale === "nl") return nl_appform_proxydesc2(inputs)
	if (locale === "fr") return fr_appform_proxydesc2(inputs)
	if (locale === "it") return it_appform_proxydesc2(inputs)
	if (locale === "hu") return hu_appform_proxydesc2(inputs)
	if (locale === "cs") return cs_appform_proxydesc2(inputs)
	if (locale === "ro") return ro_appform_proxydesc2(inputs)
	if (locale === "el") return el_appform_proxydesc2(inputs)
	if (locale === "bg") return bg_appform_proxydesc2(inputs)
	if (locale === "hr") return hr_appform_proxydesc2(inputs)
	if (locale === "sr") return sr_appform_proxydesc2(inputs)
	if (locale === "sk") return sk_appform_proxydesc2(inputs)
	return sl_appform_proxydesc2(inputs)
});
export { appform_proxydesc2 as "appForm_proxyDesc" }