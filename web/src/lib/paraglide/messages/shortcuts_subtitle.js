/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Shortcuts_SubtitleInputs */

const en_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Keyboard, mouse and link shortcuts`)
};

const sv_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Genvägar för tangentbord, mus och länkar`)
};

const uk_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Комбінації клавіш, миша та посилання`)
};

const zh_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`键盘、鼠标与链接快捷方式`)
};

const es_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Atajos de teclado, ratón y enlaces`)
};

const hi_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`कीबोर्ड, माउस और लिंक शॉर्टकट`)
};

const pt_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Atalhos de teclado, rato e ligações`)
};

const bn_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`কীবোর্ড, মাউস ও লিংক শর্টকাট`)
};

const ru_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Сочетания клавиш, мышь и ссылки`)
};

const ja_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`キーボード、マウス、リンクのショートカット`)
};

const vi_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Phím tắt, chuột và liên kết`)
};

const yue_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`鍵盤、滑鼠同連結嘅快速操作`)
};

const tr_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Klavye, fare ve bağlantı kısayolları`)
};

const ar_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`اختصارات لوحة المفاتيح والفأرة والروابط`)
};

const wuu_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`键盘、鼠标和链接快捷方式`)
};

const mr_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`कीबोर्ड, माउस आणि दुव्यांचे शॉर्टकट`)
};

const nb_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Snarveier for tastatur, mus og lenker`)
};

const fi_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Näppäimistön, hiiren ja linkkien pikatoiminnot`)
};

const da_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Genveje til tastatur, mus og links`)
};

const et_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Klaviatuuri, hiire ja linkide otseteed`)
};

const lv_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Tastatūras saīsnes, pele un saites`)
};

const lt_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Spartieji klavišai, pelė ir nuorodos`)
};

const pl_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Skróty klawiszowe, mysz i linki`)
};

const de_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Tastatur-, Maus- und Link-Kürzel`)
};

const nl_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Snelkoppelingen voor toetsenbord, muis en links`)
};

const fr_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Raccourcis clavier, souris et liens`)
};

const it_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Scorciatoie da tastiera, mouse e link`)
};

const hu_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Billentyűparancsok, egér és hivatkozások`)
};

const cs_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Klávesové zkratky, myš a odkazy`)
};

const ro_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Scurtături de tastatură, mouse și linkuri`)
};

const el_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Συντομεύσεις πληκτρολογίου, ποντικιού και συνδέσμων`)
};

const bg_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Клавишни комбинации, мишка и връзки`)
};

const hr_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Tipkovni prečaci, miš i poveznice`)
};

const sr_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Prečice na tastaturi, miš i linkovi`)
};

const sk_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Klávesové skratky, myš a odkazy`)
};

const sl_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Tipkovne bližnjice, miška in povezave`)
};

const ko_shortcuts_subtitle = /** @type {(inputs: Shortcuts_SubtitleInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`키보드, 마우스 및 링크 단축키`)
};

/**
* | output |
* | --- |
* | "Keyboard, mouse and link shortcuts" |
*
* @param {Shortcuts_SubtitleInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
export const shortcuts_subtitle = /** @type {((inputs?: Shortcuts_SubtitleInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Shortcuts_SubtitleInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "sv") return sv_shortcuts_subtitle(inputs)
	if (locale === "uk") return uk_shortcuts_subtitle(inputs)
	if (locale === "zh") return zh_shortcuts_subtitle(inputs)
	if (locale === "es") return es_shortcuts_subtitle(inputs)
	if (locale === "hi") return hi_shortcuts_subtitle(inputs)
	if (locale === "pt") return pt_shortcuts_subtitle(inputs)
	if (locale === "bn") return bn_shortcuts_subtitle(inputs)
	if (locale === "ru") return ru_shortcuts_subtitle(inputs)
	if (locale === "ja") return ja_shortcuts_subtitle(inputs)
	if (locale === "vi") return vi_shortcuts_subtitle(inputs)
	if (locale === "yue") return yue_shortcuts_subtitle(inputs)
	if (locale === "tr") return tr_shortcuts_subtitle(inputs)
	if (locale === "ar") return ar_shortcuts_subtitle(inputs)
	if (locale === "wuu") return wuu_shortcuts_subtitle(inputs)
	if (locale === "mr") return mr_shortcuts_subtitle(inputs)
	if (locale === "nb") return nb_shortcuts_subtitle(inputs)
	if (locale === "fi") return fi_shortcuts_subtitle(inputs)
	if (locale === "da") return da_shortcuts_subtitle(inputs)
	if (locale === "et") return et_shortcuts_subtitle(inputs)
	if (locale === "lv") return lv_shortcuts_subtitle(inputs)
	if (locale === "lt") return lt_shortcuts_subtitle(inputs)
	if (locale === "pl") return pl_shortcuts_subtitle(inputs)
	if (locale === "de") return de_shortcuts_subtitle(inputs)
	if (locale === "nl") return nl_shortcuts_subtitle(inputs)
	if (locale === "fr") return fr_shortcuts_subtitle(inputs)
	if (locale === "it") return it_shortcuts_subtitle(inputs)
	if (locale === "hu") return hu_shortcuts_subtitle(inputs)
	if (locale === "cs") return cs_shortcuts_subtitle(inputs)
	if (locale === "ro") return ro_shortcuts_subtitle(inputs)
	if (locale === "el") return el_shortcuts_subtitle(inputs)
	if (locale === "bg") return bg_shortcuts_subtitle(inputs)
	if (locale === "hr") return hr_shortcuts_subtitle(inputs)
	if (locale === "sr") return sr_shortcuts_subtitle(inputs)
	if (locale === "sk") return sk_shortcuts_subtitle(inputs)
	if (locale === "sl") return sl_shortcuts_subtitle(inputs)
	if (locale === "ko") return ko_shortcuts_subtitle(inputs)
	return en_shortcuts_subtitle(inputs)
});