/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Settings_GatewayInputs */

const en_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gateway`)
};

const sv_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gateway`)
};

const uk_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Шлюз`)
};

const zh_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`网关`)
};

const es_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pasarela`)
};

const hi_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`गेटवे`)
};

const pt_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gateway`)
};

const bn_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`গেটওয়ে`)
};

const ru_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Шлюз`)
};

const ja_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`ゲートウェイ`)
};

const vi_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Cổng`)
};

const yue_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`網關`)
};

const tr_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Ağ geçidi`)
};

const ar_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`بوابة`)
};

const wuu_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`网关`)
};

const mr_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`गेटवे`)
};

const nb_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gateway`)
};

const fi_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Yhdyskäytävä`)
};

const da_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gateway`)
};

const et_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lüüs`)
};

const lv_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Vārteja`)
};

const lt_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Šliuzas`)
};

const pl_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Brama`)
};

const de_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gateway`)
};

const nl_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gateway`)
};

const fr_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Passerelle`)
};

const it_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gateway`)
};

const hu_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Átjáró`)
};

const cs_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Brána`)
};

const ro_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Gateway`)
};

const el_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Πύλη`)
};

const bg_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Шлюз`)
};

const hr_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pristupnik`)
};

const sr_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Мрежни пролаз`)
};

const sk_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Brána`)
};

const sl_settings_gateway = /** @type {(inputs: Settings_GatewayInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Prehod`)
};

/**
* | output |
* | --- |
* | "Gateway" |
*
* @param {Settings_GatewayInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
export const settings_gateway = /** @type {((inputs?: Settings_GatewayInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Settings_GatewayInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_settings_gateway(inputs)
	if (locale === "sv") return sv_settings_gateway(inputs)
	if (locale === "uk") return uk_settings_gateway(inputs)
	if (locale === "zh") return zh_settings_gateway(inputs)
	if (locale === "es") return es_settings_gateway(inputs)
	if (locale === "hi") return hi_settings_gateway(inputs)
	if (locale === "pt") return pt_settings_gateway(inputs)
	if (locale === "bn") return bn_settings_gateway(inputs)
	if (locale === "ru") return ru_settings_gateway(inputs)
	if (locale === "ja") return ja_settings_gateway(inputs)
	if (locale === "vi") return vi_settings_gateway(inputs)
	if (locale === "yue") return yue_settings_gateway(inputs)
	if (locale === "tr") return tr_settings_gateway(inputs)
	if (locale === "ar") return ar_settings_gateway(inputs)
	if (locale === "wuu") return wuu_settings_gateway(inputs)
	if (locale === "mr") return mr_settings_gateway(inputs)
	if (locale === "nb") return nb_settings_gateway(inputs)
	if (locale === "fi") return fi_settings_gateway(inputs)
	if (locale === "da") return da_settings_gateway(inputs)
	if (locale === "et") return et_settings_gateway(inputs)
	if (locale === "lv") return lv_settings_gateway(inputs)
	if (locale === "lt") return lt_settings_gateway(inputs)
	if (locale === "pl") return pl_settings_gateway(inputs)
	if (locale === "de") return de_settings_gateway(inputs)
	if (locale === "nl") return nl_settings_gateway(inputs)
	if (locale === "fr") return fr_settings_gateway(inputs)
	if (locale === "it") return it_settings_gateway(inputs)
	if (locale === "hu") return hu_settings_gateway(inputs)
	if (locale === "cs") return cs_settings_gateway(inputs)
	if (locale === "ro") return ro_settings_gateway(inputs)
	if (locale === "el") return el_settings_gateway(inputs)
	if (locale === "bg") return bg_settings_gateway(inputs)
	if (locale === "hr") return hr_settings_gateway(inputs)
	if (locale === "sr") return sr_settings_gateway(inputs)
	if (locale === "sk") return sk_settings_gateway(inputs)
	return sl_settings_gateway(inputs)
});