/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Skiptlsdesc3Inputs */

const en_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const sv_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const uk_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const zh_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const es_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const hi_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const pt_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const bn_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const ru_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const ja_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const pa_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const vi_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const yue_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const tr_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const ar_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const wuu_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const mr_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const nb_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const fi_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const da_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const et_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const lv_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const lt_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const pl_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const de_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const nl_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const fr_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const it_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const hu_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const cs_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const ro_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const el_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const bg_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const hr_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const sr_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const sk_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

const sl_appform_skiptlsdesc3 = /** @type {(inputs: Appform_Skiptlsdesc3Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Disable for backends with valid certificates`)
};

/**
* | output |
* | --- |
* | "Disable for backends with valid certificates" |
*
* @param {Appform_Skiptlsdesc3Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const appform_skiptlsdesc3 = /** @type {((inputs?: Appform_Skiptlsdesc3Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Skiptlsdesc3Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_appform_skiptlsdesc3(inputs)
	if (locale === "sv") return sv_appform_skiptlsdesc3(inputs)
	if (locale === "uk") return uk_appform_skiptlsdesc3(inputs)
	if (locale === "zh") return zh_appform_skiptlsdesc3(inputs)
	if (locale === "es") return es_appform_skiptlsdesc3(inputs)
	if (locale === "hi") return hi_appform_skiptlsdesc3(inputs)
	if (locale === "pt") return pt_appform_skiptlsdesc3(inputs)
	if (locale === "bn") return bn_appform_skiptlsdesc3(inputs)
	if (locale === "ru") return ru_appform_skiptlsdesc3(inputs)
	if (locale === "ja") return ja_appform_skiptlsdesc3(inputs)
	if (locale === "pa") return pa_appform_skiptlsdesc3(inputs)
	if (locale === "vi") return vi_appform_skiptlsdesc3(inputs)
	if (locale === "yue") return yue_appform_skiptlsdesc3(inputs)
	if (locale === "tr") return tr_appform_skiptlsdesc3(inputs)
	if (locale === "ar") return ar_appform_skiptlsdesc3(inputs)
	if (locale === "wuu") return wuu_appform_skiptlsdesc3(inputs)
	if (locale === "mr") return mr_appform_skiptlsdesc3(inputs)
	if (locale === "nb") return nb_appform_skiptlsdesc3(inputs)
	if (locale === "fi") return fi_appform_skiptlsdesc3(inputs)
	if (locale === "da") return da_appform_skiptlsdesc3(inputs)
	if (locale === "et") return et_appform_skiptlsdesc3(inputs)
	if (locale === "lv") return lv_appform_skiptlsdesc3(inputs)
	if (locale === "lt") return lt_appform_skiptlsdesc3(inputs)
	if (locale === "pl") return pl_appform_skiptlsdesc3(inputs)
	if (locale === "de") return de_appform_skiptlsdesc3(inputs)
	if (locale === "nl") return nl_appform_skiptlsdesc3(inputs)
	if (locale === "fr") return fr_appform_skiptlsdesc3(inputs)
	if (locale === "it") return it_appform_skiptlsdesc3(inputs)
	if (locale === "hu") return hu_appform_skiptlsdesc3(inputs)
	if (locale === "cs") return cs_appform_skiptlsdesc3(inputs)
	if (locale === "ro") return ro_appform_skiptlsdesc3(inputs)
	if (locale === "el") return el_appform_skiptlsdesc3(inputs)
	if (locale === "bg") return bg_appform_skiptlsdesc3(inputs)
	if (locale === "hr") return hr_appform_skiptlsdesc3(inputs)
	if (locale === "sr") return sr_appform_skiptlsdesc3(inputs)
	if (locale === "sk") return sk_appform_skiptlsdesc3(inputs)
	return sl_appform_skiptlsdesc3(inputs)
});
export { appform_skiptlsdesc3 as "appForm_skipTlsDesc" }