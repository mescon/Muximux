/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} About_ReleasedInputs */

const en_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Released:`)
};

const sv_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Publicerad:`)
};

const uk_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Випущено:`)
};

const zh_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`发布时间：`)
};

const es_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Publicada:`)
};

const hi_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`रिलीज़:`)
};

const pt_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lançada:`)
};

const bn_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`প্রকাশিত:`)
};

const ru_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Выпущена:`)
};

const ja_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`リリース日：`)
};

const vi_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Phát hành:`)
};

const yue_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`發佈日期：`)
};

const tr_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Yayınlanma:`)
};

const ar_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`صدر في:`)
};

const wuu_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`发布时间：`)
};

const mr_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`प्रकाशित:`)
};

const nb_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Utgitt:`)
};

const fi_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Julkaistu:`)
};

const da_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Udgivet:`)
};

const et_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Avaldatud:`)
};

const lv_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Izdots:`)
};

const lt_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Išleista:`)
};

const pl_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Wydana:`)
};

const de_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Veröffentlicht:`)
};

const nl_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Uitgebracht:`)
};

const fr_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Publiée :`)
};

const it_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Rilasciata:`)
};

const hu_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Kiadva:`)
};

const cs_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Vydáno:`)
};

const ro_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lansat:`)
};

const el_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Κυκλοφορία:`)
};

const bg_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Издадена:`)
};

const hr_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Objavljeno:`)
};

const sr_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Objavljeno:`)
};

const sk_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Vydaná:`)
};

const sl_about_released = /** @type {(inputs: About_ReleasedInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Izdano:`)
};

/**
* | output |
* | --- |
* | "Released:" |
*
* @param {About_ReleasedInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
export const about_released = /** @type {((inputs?: About_ReleasedInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<About_ReleasedInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_about_released(inputs)
	if (locale === "sv") return sv_about_released(inputs)
	if (locale === "uk") return uk_about_released(inputs)
	if (locale === "zh") return zh_about_released(inputs)
	if (locale === "es") return es_about_released(inputs)
	if (locale === "hi") return hi_about_released(inputs)
	if (locale === "pt") return pt_about_released(inputs)
	if (locale === "bn") return bn_about_released(inputs)
	if (locale === "ru") return ru_about_released(inputs)
	if (locale === "ja") return ja_about_released(inputs)
	if (locale === "vi") return vi_about_released(inputs)
	if (locale === "yue") return yue_about_released(inputs)
	if (locale === "tr") return tr_about_released(inputs)
	if (locale === "ar") return ar_about_released(inputs)
	if (locale === "wuu") return wuu_about_released(inputs)
	if (locale === "mr") return mr_about_released(inputs)
	if (locale === "nb") return nb_about_released(inputs)
	if (locale === "fi") return fi_about_released(inputs)
	if (locale === "da") return da_about_released(inputs)
	if (locale === "et") return et_about_released(inputs)
	if (locale === "lv") return lv_about_released(inputs)
	if (locale === "lt") return lt_about_released(inputs)
	if (locale === "pl") return pl_about_released(inputs)
	if (locale === "de") return de_about_released(inputs)
	if (locale === "nl") return nl_about_released(inputs)
	if (locale === "fr") return fr_about_released(inputs)
	if (locale === "it") return it_about_released(inputs)
	if (locale === "hu") return hu_about_released(inputs)
	if (locale === "cs") return cs_about_released(inputs)
	if (locale === "ro") return ro_about_released(inputs)
	if (locale === "el") return el_about_released(inputs)
	if (locale === "bg") return bg_about_released(inputs)
	if (locale === "hr") return hr_about_released(inputs)
	if (locale === "sr") return sr_about_released(inputs)
	if (locale === "sk") return sk_about_released(inputs)
	return sl_about_released(inputs)
});