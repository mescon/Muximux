/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} General_ShadowInputs */

const en_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const sv_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const uk_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const zh_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const es_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const hi_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const pt_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const bn_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const ru_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const ja_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const pa_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const vi_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const yue_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const tr_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const ar_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const wuu_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const mr_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const nb_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const fi_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const da_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const et_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const lv_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const lt_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const pl_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const de_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const nl_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const fr_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const it_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const hu_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const cs_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const ro_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const el_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const bg_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const hr_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const sr_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const sk_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

const sl_general_shadow = /** @type {(inputs: General_ShadowInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Shadow — show a drop shadow on the expanded menu`)
};

/**
* | output |
* | --- |
* | "Shadow — show a drop shadow on the expanded menu" |
*
* @param {General_ShadowInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
export const general_shadow = /** @type {((inputs?: General_ShadowInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<General_ShadowInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_general_shadow(inputs)
	if (locale === "sv") return sv_general_shadow(inputs)
	if (locale === "uk") return uk_general_shadow(inputs)
	if (locale === "zh") return zh_general_shadow(inputs)
	if (locale === "es") return es_general_shadow(inputs)
	if (locale === "hi") return hi_general_shadow(inputs)
	if (locale === "pt") return pt_general_shadow(inputs)
	if (locale === "bn") return bn_general_shadow(inputs)
	if (locale === "ru") return ru_general_shadow(inputs)
	if (locale === "ja") return ja_general_shadow(inputs)
	if (locale === "pa") return pa_general_shadow(inputs)
	if (locale === "vi") return vi_general_shadow(inputs)
	if (locale === "yue") return yue_general_shadow(inputs)
	if (locale === "tr") return tr_general_shadow(inputs)
	if (locale === "ar") return ar_general_shadow(inputs)
	if (locale === "wuu") return wuu_general_shadow(inputs)
	if (locale === "mr") return mr_general_shadow(inputs)
	if (locale === "nb") return nb_general_shadow(inputs)
	if (locale === "fi") return fi_general_shadow(inputs)
	if (locale === "da") return da_general_shadow(inputs)
	if (locale === "et") return et_general_shadow(inputs)
	if (locale === "lv") return lv_general_shadow(inputs)
	if (locale === "lt") return lt_general_shadow(inputs)
	if (locale === "pl") return pl_general_shadow(inputs)
	if (locale === "de") return de_general_shadow(inputs)
	if (locale === "nl") return nl_general_shadow(inputs)
	if (locale === "fr") return fr_general_shadow(inputs)
	if (locale === "it") return it_general_shadow(inputs)
	if (locale === "hu") return hu_general_shadow(inputs)
	if (locale === "cs") return cs_general_shadow(inputs)
	if (locale === "ro") return ro_general_shadow(inputs)
	if (locale === "el") return el_general_shadow(inputs)
	if (locale === "bg") return bg_general_shadow(inputs)
	if (locale === "hr") return hr_general_shadow(inputs)
	if (locale === "sr") return sr_general_shadow(inputs)
	if (locale === "sk") return sk_general_shadow(inputs)
	return sl_general_shadow(inputs)
});