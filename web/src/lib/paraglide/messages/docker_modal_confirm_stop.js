/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{ name: NonNullable<unknown> }} Docker_Modal_Confirm_StopInputs */

const en_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Stop container '${i?.name}'?`)
};

const sv_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Stoppa container '${i?.name}'?`)
};

const uk_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Зупинити контейнер '${i?.name}'?`)
};

const zh_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`停止容器 '${i?.name}'？`)
};

const es_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`¿Detener el contenedor '${i?.name}'?`)
};

const hi_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`कंटेनर '${i?.name}' को रोकें?`)
};

const pt_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Parar o contentor '${i?.name}'?`)
};

const bn_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`কন্টেইনার '${i?.name}' বন্ধ করবেন?`)
};

const ru_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Остановить контейнер «${i?.name}»?`)
};

const ja_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`コンテナ '${i?.name}' を停止しますか？`)
};

const vi_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Dừng container '${i?.name}'?`)
};

const yue_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`停止容器 '${i?.name}'？`)
};

const tr_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`'${i?.name}' kapsayıcısı durdurulsun mu?`)
};

const ar_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`إيقاف الحاوية '${i?.name}'؟`)
};

const wuu_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`停止容器 '${i?.name}' 𠲎？`)
};

const mr_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`कंटेनर '${i?.name}' थांबवायचे?`)
};

const nb_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Stopp container '${i?.name}'?`)
};

const fi_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Pysäytetäänkö säiliö '${i?.name}'?`)
};

const da_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Stop container '${i?.name}'?`)
};

const et_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Kas peatada konteiner '${i?.name}'?`)
};

const lv_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Apturēt konteineru '${i?.name}'?`)
};

const lt_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Sustabdyti konteinerį '${i?.name}'?`)
};

const pl_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Zatrzymać kontener '${i?.name}'?`)
};

const de_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Container '${i?.name}' stoppen?`)
};

const nl_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Container '${i?.name}' stoppen?`)
};

const fr_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Arrêter le conteneur '${i?.name}' ?`)
};

const it_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Arrestare il container '${i?.name}'?`)
};

const hu_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Leállítja a(z) '${i?.name}' konténert?`)
};

const cs_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Zastavit kontejner '${i?.name}'?`)
};

const ro_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Oprești containerul '${i?.name}'?`)
};

const el_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Διακοπή του container '${i?.name}';`)
};

const bg_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Да се спре ли контейнерът '${i?.name}'?`)
};

const hr_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Zaustaviti kontejner '${i?.name}'?`)
};

const sr_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Zaustaviti kontejner '${i?.name}'?`)
};

const sk_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Zastaviť kontajner '${i?.name}'?`)
};

const sl_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Ustaviti vsebnik '${i?.name}'?`)
};

const ko_docker_modal_confirm_stop = /** @type {(inputs: Docker_Modal_Confirm_StopInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`'${i?.name}' 컨테이너를 중지하시겠습니까?`)
};

/**
* | output |
* | --- |
* | "Stop container '{name}'?" |
*
* @param {Docker_Modal_Confirm_StopInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
export const docker_modal_confirm_stop = /** @type {((inputs: Docker_Modal_Confirm_StopInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Docker_Modal_Confirm_StopInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_docker_modal_confirm_stop(inputs)
	if (locale === "sv") return sv_docker_modal_confirm_stop(inputs)
	if (locale === "uk") return uk_docker_modal_confirm_stop(inputs)
	if (locale === "zh") return zh_docker_modal_confirm_stop(inputs)
	if (locale === "es") return es_docker_modal_confirm_stop(inputs)
	if (locale === "hi") return hi_docker_modal_confirm_stop(inputs)
	if (locale === "pt") return pt_docker_modal_confirm_stop(inputs)
	if (locale === "bn") return bn_docker_modal_confirm_stop(inputs)
	if (locale === "ru") return ru_docker_modal_confirm_stop(inputs)
	if (locale === "ja") return ja_docker_modal_confirm_stop(inputs)
	if (locale === "vi") return vi_docker_modal_confirm_stop(inputs)
	if (locale === "yue") return yue_docker_modal_confirm_stop(inputs)
	if (locale === "tr") return tr_docker_modal_confirm_stop(inputs)
	if (locale === "ar") return ar_docker_modal_confirm_stop(inputs)
	if (locale === "wuu") return wuu_docker_modal_confirm_stop(inputs)
	if (locale === "mr") return mr_docker_modal_confirm_stop(inputs)
	if (locale === "nb") return nb_docker_modal_confirm_stop(inputs)
	if (locale === "fi") return fi_docker_modal_confirm_stop(inputs)
	if (locale === "da") return da_docker_modal_confirm_stop(inputs)
	if (locale === "et") return et_docker_modal_confirm_stop(inputs)
	if (locale === "lv") return lv_docker_modal_confirm_stop(inputs)
	if (locale === "lt") return lt_docker_modal_confirm_stop(inputs)
	if (locale === "pl") return pl_docker_modal_confirm_stop(inputs)
	if (locale === "de") return de_docker_modal_confirm_stop(inputs)
	if (locale === "nl") return nl_docker_modal_confirm_stop(inputs)
	if (locale === "fr") return fr_docker_modal_confirm_stop(inputs)
	if (locale === "it") return it_docker_modal_confirm_stop(inputs)
	if (locale === "hu") return hu_docker_modal_confirm_stop(inputs)
	if (locale === "cs") return cs_docker_modal_confirm_stop(inputs)
	if (locale === "ro") return ro_docker_modal_confirm_stop(inputs)
	if (locale === "el") return el_docker_modal_confirm_stop(inputs)
	if (locale === "bg") return bg_docker_modal_confirm_stop(inputs)
	if (locale === "hr") return hr_docker_modal_confirm_stop(inputs)
	if (locale === "sr") return sr_docker_modal_confirm_stop(inputs)
	if (locale === "sk") return sk_docker_modal_confirm_stop(inputs)
	if (locale === "sl") return sl_docker_modal_confirm_stop(inputs)
	return ko_docker_modal_confirm_stop(inputs)
});