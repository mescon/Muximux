/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{ image: NonNullable<unknown>, uptimeOrExit: NonNullable<unknown> }} Docker_Modal_BodyInputs */

const en_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const sv_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const uk_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Образ: ${i?.image} - ${i?.uptimeOrExit}`)
};

const zh_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const es_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Imagen: ${i?.image} - ${i?.uptimeOrExit}`)
};

const hi_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const pt_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Imagem: ${i?.image} - ${i?.uptimeOrExit}`)
};

const bn_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const ru_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Образ: ${i?.image} - ${i?.uptimeOrExit}`)
};

const ja_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const vi_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const yue_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`鏡像：${i?.image} - ${i?.uptimeOrExit}`)
};

const tr_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`İmaj: ${i?.image} - ${i?.uptimeOrExit}`)
};

const ar_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`الصورة: ${i?.image} - ${i?.uptimeOrExit}`)
};

const wuu_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`镜像：${i?.image} - ${i?.uptimeOrExit}`)
};

const mr_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const nb_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const fi_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Vedos: ${i?.image} - ${i?.uptimeOrExit}`)
};

const da_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const et_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Tõmmis: ${i?.image} - ${i?.uptimeOrExit}`)
};

const lv_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Attēls: ${i?.image} - ${i?.uptimeOrExit}`)
};

const lt_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Atvaizdas: ${i?.image} - ${i?.uptimeOrExit}`)
};

const pl_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Obraz: ${i?.image} - ${i?.uptimeOrExit}`)
};

const de_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const nl_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image: ${i?.image} - ${i?.uptimeOrExit}`)
};

const fr_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Image : ${i?.image} - ${i?.uptimeOrExit}`)
};

const it_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Immagine: ${i?.image} - ${i?.uptimeOrExit}`)
};

const hu_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Lemezkép: ${i?.image} - ${i?.uptimeOrExit}`)
};

const cs_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Obraz: ${i?.image} - ${i?.uptimeOrExit}`)
};

const ro_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Imagine: ${i?.image} - ${i?.uptimeOrExit}`)
};

const el_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Εικόνα: ${i?.image} - ${i?.uptimeOrExit}`)
};

const bg_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Образ: ${i?.image} - ${i?.uptimeOrExit}`)
};

const hr_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Slika: ${i?.image} - ${i?.uptimeOrExit}`)
};

const sr_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Slika: ${i?.image} - ${i?.uptimeOrExit}`)
};

const sk_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Obraz: ${i?.image} - ${i?.uptimeOrExit}`)
};

const sl_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Slika: ${i?.image} - ${i?.uptimeOrExit}`)
};

const ko_docker_modal_body = /** @type {(inputs: Docker_Modal_BodyInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`이미지: ${i?.image} - ${i?.uptimeOrExit}`)
};

/**
* | output |
* | --- |
* | "Image: {image} - {uptimeOrExit}" |
*
* @param {Docker_Modal_BodyInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
export const docker_modal_body = /** @type {((inputs: Docker_Modal_BodyInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Docker_Modal_BodyInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_docker_modal_body(inputs)
	if (locale === "sv") return sv_docker_modal_body(inputs)
	if (locale === "uk") return uk_docker_modal_body(inputs)
	if (locale === "zh") return zh_docker_modal_body(inputs)
	if (locale === "es") return es_docker_modal_body(inputs)
	if (locale === "hi") return hi_docker_modal_body(inputs)
	if (locale === "pt") return pt_docker_modal_body(inputs)
	if (locale === "bn") return bn_docker_modal_body(inputs)
	if (locale === "ru") return ru_docker_modal_body(inputs)
	if (locale === "ja") return ja_docker_modal_body(inputs)
	if (locale === "vi") return vi_docker_modal_body(inputs)
	if (locale === "yue") return yue_docker_modal_body(inputs)
	if (locale === "tr") return tr_docker_modal_body(inputs)
	if (locale === "ar") return ar_docker_modal_body(inputs)
	if (locale === "wuu") return wuu_docker_modal_body(inputs)
	if (locale === "mr") return mr_docker_modal_body(inputs)
	if (locale === "nb") return nb_docker_modal_body(inputs)
	if (locale === "fi") return fi_docker_modal_body(inputs)
	if (locale === "da") return da_docker_modal_body(inputs)
	if (locale === "et") return et_docker_modal_body(inputs)
	if (locale === "lv") return lv_docker_modal_body(inputs)
	if (locale === "lt") return lt_docker_modal_body(inputs)
	if (locale === "pl") return pl_docker_modal_body(inputs)
	if (locale === "de") return de_docker_modal_body(inputs)
	if (locale === "nl") return nl_docker_modal_body(inputs)
	if (locale === "fr") return fr_docker_modal_body(inputs)
	if (locale === "it") return it_docker_modal_body(inputs)
	if (locale === "hu") return hu_docker_modal_body(inputs)
	if (locale === "cs") return cs_docker_modal_body(inputs)
	if (locale === "ro") return ro_docker_modal_body(inputs)
	if (locale === "el") return el_docker_modal_body(inputs)
	if (locale === "bg") return bg_docker_modal_body(inputs)
	if (locale === "hr") return hr_docker_modal_body(inputs)
	if (locale === "sr") return sr_docker_modal_body(inputs)
	if (locale === "sk") return sk_docker_modal_body(inputs)
	if (locale === "sl") return sl_docker_modal_body(inputs)
	return ko_docker_modal_body(inputs)
});