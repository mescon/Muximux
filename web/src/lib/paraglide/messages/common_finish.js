/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Common_FinishInputs */

const en_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Finish`)
};

const sv_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Slutför`)
};

const uk_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Завершити`)
};

const zh_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`完成`)
};

const es_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Finalizar`)
};

const hi_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`समाप्त करें`)
};

const pt_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Terminar`)
};

const bn_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`শেষ করুন`)
};

const ru_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Завершить`)
};

const ja_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`完了`)
};

const vi_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Hoàn tất`)
};

const yue_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`完成`)
};

const tr_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Bitir`)
};

const ar_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`إنهاء`)
};

const wuu_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`完成`)
};

const mr_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`समाप्त`)
};

const nb_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Fullfør`)
};

const fi_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Viimeistele`)
};

const da_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Afslut`)
};

const et_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lõpeta`)
};

const lv_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pabeigt`)
};

const lt_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Baigti`)
};

const pl_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zakończ`)
};

const de_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Abschließen`)
};

const nl_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Voltooien`)
};

const fr_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Terminer`)
};

const it_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Termina`)
};

const hu_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Befejezés`)
};

const cs_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Dokončit`)
};

const ro_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Finalizare`)
};

const el_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Τέλος`)
};

const bg_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Завърши`)
};

const hr_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Završi`)
};

const sr_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Završi`)
};

const sk_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Dokončiť`)
};

const sl_common_finish = /** @type {(inputs: Common_FinishInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zaključi`)
};

/**
* | output |
* | --- |
* | "Finish" |
*
* @param {Common_FinishInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
export const common_finish = /** @type {((inputs?: Common_FinishInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Common_FinishInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_common_finish(inputs)
	if (locale === "sv") return sv_common_finish(inputs)
	if (locale === "uk") return uk_common_finish(inputs)
	if (locale === "zh") return zh_common_finish(inputs)
	if (locale === "es") return es_common_finish(inputs)
	if (locale === "hi") return hi_common_finish(inputs)
	if (locale === "pt") return pt_common_finish(inputs)
	if (locale === "bn") return bn_common_finish(inputs)
	if (locale === "ru") return ru_common_finish(inputs)
	if (locale === "ja") return ja_common_finish(inputs)
	if (locale === "vi") return vi_common_finish(inputs)
	if (locale === "yue") return yue_common_finish(inputs)
	if (locale === "tr") return tr_common_finish(inputs)
	if (locale === "ar") return ar_common_finish(inputs)
	if (locale === "wuu") return wuu_common_finish(inputs)
	if (locale === "mr") return mr_common_finish(inputs)
	if (locale === "nb") return nb_common_finish(inputs)
	if (locale === "fi") return fi_common_finish(inputs)
	if (locale === "da") return da_common_finish(inputs)
	if (locale === "et") return et_common_finish(inputs)
	if (locale === "lv") return lv_common_finish(inputs)
	if (locale === "lt") return lt_common_finish(inputs)
	if (locale === "pl") return pl_common_finish(inputs)
	if (locale === "de") return de_common_finish(inputs)
	if (locale === "nl") return nl_common_finish(inputs)
	if (locale === "fr") return fr_common_finish(inputs)
	if (locale === "it") return it_common_finish(inputs)
	if (locale === "hu") return hu_common_finish(inputs)
	if (locale === "cs") return cs_common_finish(inputs)
	if (locale === "ro") return ro_common_finish(inputs)
	if (locale === "el") return el_common_finish(inputs)
	if (locale === "bg") return bg_common_finish(inputs)
	if (locale === "hr") return hr_common_finish(inputs)
	if (locale === "sr") return sr_common_finish(inputs)
	if (locale === "sk") return sk_common_finish(inputs)
	return sl_common_finish(inputs)
});