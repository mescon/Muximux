/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Helpproxy2Inputs */

const en_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Routes all traffic through the built-in Caddy reverse proxy. The app URL is rewritten to a local <code>/proxy/app-name/</code> path, avoiding CORS, mixed-content, and cookie-domain issues.`)
};

const sv_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Dirigerar all trafik genom den inbyggda Caddy-omvända proxyn. App-URL:en skrivs om till en lokal <code>/proxy/app-name/</code>-sökväg, vilket undviker CORS-, blandat-innehåll- och cookie-domänproblem.`)
};

const uk_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Маршрутизує весь трафік через вбудований Caddy reverse proxy. URL застосунку перезаписується на локальний шлях <code>/proxy/app-name/</code>, уникаючи проблем з CORS, mixed-content та cookie-domain.`)
};

const zh_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`通过内置 Caddy 反向代理路由所有流量。应用 URL 被重写为本地 <code>/proxy/app-name/</code> 路径，避免 CORS、混合内容和 cookie 域名问题。`)
};

const es_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Enruta todo el tráfico a través del reverse proxy Caddy integrado. La URL de la app se reescribe a una ruta local <code>/proxy/nombre-de-app/</code>, evitando problemas de CORS, contenido mixto y dominios de cookies.`)
};

const hi_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`सभी ट्रैफ़िक को बिल्ट-इन Caddy reverse proxy के माध्यम से रूट करता है। ऐप URL को स्थानीय <code>/proxy/app-name/</code> पथ पर फिर से लिखा जाता है, जिससे CORS, मिश्रित-सामग्री और कुकी-डोमेन समस्याएं टल जाती हैं।`)
};

const pt_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Encaminha todo o tráfego pelo reverse proxy Caddy integrado. O URL da app é reescrito para um caminho local <code>/proxy/nome-da-app/</code>, evitando problemas de CORS, conteúdo misto e domínios de cookies.`)
};

const bn_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`সমস্ত ট্র্যাফিক বিল্ট-ইন Caddy reverse proxy-এর মাধ্যমে রুট করে। অ্যাপ URL স্থানীয় <code>/proxy/app-name/</code> পাথে পুনর্লিখিত হয়, CORS, মিশ্র-কন্টেন্ট এবং কুকি-ডোমেন সমস্যা এড়ায়।`)
};

const ru_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Маршрутизирует весь трафик через встроенный Caddy reverse proxy. URL приложения переписывается на локальный путь <code>/proxy/app-name/</code>, что решает проблемы с CORS, смешанным контентом и доменами cookie.`)
};

const ja_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`内蔵の Caddy リバースプロキシ経由ですべてのトラフィックをルーティングします。アプリ URL はローカルの <code>/proxy/app-name/</code> パスに書き換えられ、CORS、混合コンテンツ、cookie ドメインの問題を回避します。`)
};

const vi_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Định tuyến tất cả lưu lượng qua Caddy reverse proxy tích hợp. URL ứng dụng được viết lại thành đường dẫn cục bộ <code>/proxy/app-name/</code>, tránh các vấn đề CORS, nội dung hỗn hợp và miền cookie.`)
};

const yue_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`透過內置 Caddy 反向代理路由所有流量。應用程式 URL 會被改寫為本機 <code>/proxy/app-name/</code> 路徑，避免 CORS、混合內容同 cookie 域名問題。`)
};

const tr_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Tüm trafiği yerleşik Caddy reverse proxy üzerinden yönlendirir. Uygulama URL'si yerel <code>/proxy/app-name/</code> yoluna yeniden yazılır, böylece CORS, karışık içerik ve çerez alan adı sorunları önlenir.`)
};

const ar_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`يوجّه كل حركة المرور عبر Caddy reverse proxy المدمج. يتم إعادة كتابة عنوان URL للتطبيق إلى مسار محلي <code>/proxy/app-name/</code>، مما يتجنب مشاكل CORS والمحتوى المختلط ونطاقات ملفات تعريف الارتباط.`)
};

const wuu_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`所有流量通过内置 Caddy 反向代理转发。应用 URL 会被改写为本地 <code>/proxy/app-name/</code> 路径，避免 CORS、混合内容和 cookie 域名问题。`)
};

const mr_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`सर्व ट्रॅफिक अंगभूत Caddy रिव्हर्स प्रॉक्सीद्वारे रूट होतो. अॅप URL स्थानिक <code>/proxy/app-name/</code> पथावर पुनर्लेखित होतो, CORS, मिश्र-सामग्री आणि कुकी-डोमेन समस्या टाळतो.`)
};

const nb_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Ruter all trafikk gjennom den innebygde Caddy omvendte proxyen. App-URL-en omskrives til en lokal <code>/proxy/app-name/</code>-sti, som unngår CORS-, blandet-innhold- og cookie-domeneproblemer.`)
};

const fi_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Reitittää kaiken liikenteen sisäänrakennetun Caddy-käänteisproxyn kautta. Sovelluksen URL kirjoitetaan uudelleen paikalliseen <code>/proxy/app-name/</code>-polkuun, joka välttää CORS-, sekoitettu-sisältö- ja evästealueongelmat.`)
};

const da_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Ruter al trafik gennem den indbyggede Caddy omvendte proxy. App-URL'en omskrives til en lokal <code>/proxy/app-name/</code>-sti, som undgår CORS-, blandet-indhold- og cookie-domæneproblemer.`)
};

const et_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Suunab kogu liikluse läbi sisseehitatud Caddy pöördproxy. Rakenduse URL kirjutatakse ümber kohalikuks <code>/proxy/app-name/</code> teeks, vältides CORS, segasisu ja küpsise domeeni probleeme.`)
};

const lv_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Maršrutē visu datplūsmu caur iebūvēto Caddy reverso proxy. Lietotnes URL tiek pārrakstīts uz lokālu <code>/proxy/app-name/</code> ceļu, izvairoties no CORS, jauktā satura un sīkdatņu domēna problēmām.`)
};

const lt_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Nukreipia visą srautą per įmontuotą Caddy reverse proxy. Programos URL perrašomas į vietinį <code>/proxy/app-name/</code> kelią, išvengiant CORS, mixed-content ir cookie-domain problemų.`)
};

const pl_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Kieruje cały ruch przez wbudowany reverse proxy Caddy. URL aplikacji jest przepisywany na lokalną ścieżkę <code>/proxy/app-name/</code>, unikając problemów z CORS, mieszanym kontekstem i domenami ciasteczek.`)
};

const de_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Leitet den gesamten Datenverkehr über den eingebauten Caddy Reverse Proxy. Die App-URL wird in einen lokalen <code>/proxy/app-name/</code>-Pfad umgeschrieben, um CORS-, Mixed-Content- und Cookie-Domain-Probleme zu vermeiden.`)
};

const nl_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Leidt al het verkeer door de ingebouwde Caddy reverse proxy. De app-URL wordt herschreven naar een lokaal <code>/proxy/app-name/</code>-pad, om CORS-, mixed-content- en cookie-domeinproblemen te vermijden.`)
};

const fr_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Achemine tout le trafic via le reverse proxy Caddy intégré. L'URL de l'app est réécrite vers un chemin local <code>/proxy/app-name/</code>, évitant les problèmes de CORS, de contenu mixte et de domaine de cookies.`)
};

const it_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Instrada tutto il traffico attraverso il reverse proxy Caddy integrato. L'URL dell'app viene riscritta in un percorso locale <code>/proxy/app-name/</code>, evitando problemi di CORS, contenuto misto e dominio dei cookie.`)
};

const hu_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Az összes forgalmat a beépített Caddy reverse proxy-n keresztül irányítja. Az alkalmazás URL-je helyi <code>/proxy/app-name/</code> útvonalra íródik át, elkerülve a CORS, vegyes tartalom és cookie-domain problémákat.`)
};

const cs_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Směruje veškerý provoz přes vestavěný reverse proxy Caddy. URL aplikace je přepsána na lokální cestu <code>/proxy/app-name/</code>, čímž se vyhne problémům s CORS, smíšeným obsahem a doménami cookies.`)
};

const ro_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Direcționează tot traficul prin reverse proxy-ul Caddy încorporat. URL-ul aplicației este rescris într-o cale locală <code>/proxy/app-name/</code>, evitând problemele de CORS, conținut mixt și domeniu cookie.`)
};

const el_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Δρομολογεί όλη την κίνηση μέσω του ενσωματωμένου Caddy reverse proxy. Το URL της εφαρμογής ξαναγράφεται σε τοπικό μονοπάτι <code>/proxy/app-name/</code>, αποφεύγοντας προβλήματα CORS, mixed-content και cookie-domain.`)
};

const bg_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Насочва целия трафик през вградения Caddy reverse proxy. URL адресът на приложението се пренаписва до локален път <code>/proxy/app-name/</code>, избягвайки проблеми с CORS, смесено съдържание и домейн на бисквитки.`)
};

const hr_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Usmjerava sav promet kroz ugrađeni Caddy obrnuti proxy. URL aplikacije se prepisuje na lokalnu <code>/proxy/app-name/</code> putanju, izbjegavajući CORS, mješoviti sadržaj i probleme s domenama kolačića.`)
};

const sr_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Usmerava sav saobraćaj kroz ugrađeni Caddy obrnuti proxy. URL aplikacije se prepisuje na lokalnu <code>/proxy/app-name/</code> putanju, izbegavajući CORS, mešoviti sadržaj i probleme sa domenima kolačića.`)
};

const sk_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Smeruje všetku komunikáciu cez vstavaný reverse proxy Caddy. URL aplikácie je prepísaná na lokálnu cestu <code>/proxy/app-name/</code>, čím sa vyhnete problémom s CORS, zmiešaným obsahom a doménami cookies.`)
};

const sl_appform_helpproxy2 = /** @type {(inputs: Appform_Helpproxy2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Usmeri ves promet skozi vgrajeni Caddy obrnjeni proxy. URL aplikacije se prepiše na lokalno <code>/proxy/app-name/</code> pot, s čimer se izogne CORS, mešani vsebini in težavam z domenami piškotkov.`)
};

/**
* | output |
* | --- |
* | "Routes all traffic through the built-in Caddy reverse proxy. The app URL is rewritten to a local <code>/proxy/app-name/</code> path, avoiding CORS, mixed-con..." |
*
* @param {Appform_Helpproxy2Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const appform_helpproxy2 = /** @type {((inputs?: Appform_Helpproxy2Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Helpproxy2Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_appform_helpproxy2(inputs)
	if (locale === "sv") return sv_appform_helpproxy2(inputs)
	if (locale === "uk") return uk_appform_helpproxy2(inputs)
	if (locale === "zh") return zh_appform_helpproxy2(inputs)
	if (locale === "es") return es_appform_helpproxy2(inputs)
	if (locale === "hi") return hi_appform_helpproxy2(inputs)
	if (locale === "pt") return pt_appform_helpproxy2(inputs)
	if (locale === "bn") return bn_appform_helpproxy2(inputs)
	if (locale === "ru") return ru_appform_helpproxy2(inputs)
	if (locale === "ja") return ja_appform_helpproxy2(inputs)
	if (locale === "vi") return vi_appform_helpproxy2(inputs)
	if (locale === "yue") return yue_appform_helpproxy2(inputs)
	if (locale === "tr") return tr_appform_helpproxy2(inputs)
	if (locale === "ar") return ar_appform_helpproxy2(inputs)
	if (locale === "wuu") return wuu_appform_helpproxy2(inputs)
	if (locale === "mr") return mr_appform_helpproxy2(inputs)
	if (locale === "nb") return nb_appform_helpproxy2(inputs)
	if (locale === "fi") return fi_appform_helpproxy2(inputs)
	if (locale === "da") return da_appform_helpproxy2(inputs)
	if (locale === "et") return et_appform_helpproxy2(inputs)
	if (locale === "lv") return lv_appform_helpproxy2(inputs)
	if (locale === "lt") return lt_appform_helpproxy2(inputs)
	if (locale === "pl") return pl_appform_helpproxy2(inputs)
	if (locale === "de") return de_appform_helpproxy2(inputs)
	if (locale === "nl") return nl_appform_helpproxy2(inputs)
	if (locale === "fr") return fr_appform_helpproxy2(inputs)
	if (locale === "it") return it_appform_helpproxy2(inputs)
	if (locale === "hu") return hu_appform_helpproxy2(inputs)
	if (locale === "cs") return cs_appform_helpproxy2(inputs)
	if (locale === "ro") return ro_appform_helpproxy2(inputs)
	if (locale === "el") return el_appform_helpproxy2(inputs)
	if (locale === "bg") return bg_appform_helpproxy2(inputs)
	if (locale === "hr") return hr_appform_helpproxy2(inputs)
	if (locale === "sr") return sr_appform_helpproxy2(inputs)
	if (locale === "sk") return sk_appform_helpproxy2(inputs)
	return sl_appform_helpproxy2(inputs)
});
export { appform_helpproxy2 as "appForm_helpProxy" }