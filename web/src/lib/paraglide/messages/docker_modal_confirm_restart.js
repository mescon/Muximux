/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{ name: NonNullable<unknown> }} Docker_Modal_Confirm_RestartInputs */

const en_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Restart container '${i?.name}'?`)
};

const sv_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Starta om container '${i?.name}'?`)
};

const uk_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Перезапустити контейнер '${i?.name}'?`)
};

const zh_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`重启容器 '${i?.name}'？`)
};

const es_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`¿Reiniciar el contenedor '${i?.name}'?`)
};

const hi_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`कंटेनर '${i?.name}' को पुनरारंभ करें?`)
};

const pt_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Reiniciar o contentor '${i?.name}'?`)
};

const bn_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`কন্টেইনার '${i?.name}' পুনরায় চালু করবেন?`)
};

const ru_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Перезапустить контейнер «${i?.name}»?`)
};

const ja_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`コンテナ '${i?.name}' を再起動しますか？`)
};

const vi_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Khởi động lại container '${i?.name}'?`)
};

const yue_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`重新啟動容器 '${i?.name}'？`)
};

const tr_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`'${i?.name}' kapsayıcısı yeniden başlatılsın mı?`)
};

const ar_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`إعادة تشغيل الحاوية '${i?.name}'؟`)
};

const wuu_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`重启容器 '${i?.name}' 𠲎？`)
};

const mr_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`कंटेनर '${i?.name}' पुन्हा सुरू करायचे?`)
};

const nb_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Start container '${i?.name}' på nytt?`)
};

const fi_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Käynnistetäänkö säiliö '${i?.name}' uudelleen?`)
};

const da_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Genstart container '${i?.name}'?`)
};

const et_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Kas taaskäivitada konteiner '${i?.name}'?`)
};

const lv_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Restartēt konteineru '${i?.name}'?`)
};

const lt_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Paleisti konteinerį '${i?.name}' iš naujo?`)
};

const pl_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Uruchomić ponownie kontener '${i?.name}'?`)
};

const de_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Container '${i?.name}' neu starten?`)
};

const nl_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Container '${i?.name}' opnieuw starten?`)
};

const fr_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Redémarrer le conteneur '${i?.name}' ?`)
};

const it_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Riavviare il container '${i?.name}'?`)
};

const hu_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Újraindítja a(z) '${i?.name}' konténert?`)
};

const cs_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Restartovat kontejner '${i?.name}'?`)
};

const ro_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Repornești containerul '${i?.name}'?`)
};

const el_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Επανεκκίνηση του container '${i?.name}';`)
};

const bg_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Да се рестартира ли контейнерът '${i?.name}'?`)
};

const hr_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Ponovno pokrenuti kontejner '${i?.name}'?`)
};

const sr_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Ponovo pokrenuti kontejner '${i?.name}'?`)
};

const sk_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Reštartovať kontajner '${i?.name}'?`)
};

const sl_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`Znova zagnati vsebnik '${i?.name}'?`)
};

const ko_docker_modal_confirm_restart = /** @type {(inputs: Docker_Modal_Confirm_RestartInputs) => LocalizedString} */ (i) => {
	return /** @type {LocalizedString} */ (`'${i?.name}' 컨테이너를 재시작하시겠습니까?`)
};

/**
* | output |
* | --- |
* | "Restart container '{name}'?" |
*
* @param {Docker_Modal_Confirm_RestartInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
export const docker_modal_confirm_restart = /** @type {((inputs: Docker_Modal_Confirm_RestartInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Docker_Modal_Confirm_RestartInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "sv") return sv_docker_modal_confirm_restart(inputs)
	if (locale === "uk") return uk_docker_modal_confirm_restart(inputs)
	if (locale === "zh") return zh_docker_modal_confirm_restart(inputs)
	if (locale === "es") return es_docker_modal_confirm_restart(inputs)
	if (locale === "hi") return hi_docker_modal_confirm_restart(inputs)
	if (locale === "pt") return pt_docker_modal_confirm_restart(inputs)
	if (locale === "bn") return bn_docker_modal_confirm_restart(inputs)
	if (locale === "ru") return ru_docker_modal_confirm_restart(inputs)
	if (locale === "ja") return ja_docker_modal_confirm_restart(inputs)
	if (locale === "vi") return vi_docker_modal_confirm_restart(inputs)
	if (locale === "yue") return yue_docker_modal_confirm_restart(inputs)
	if (locale === "tr") return tr_docker_modal_confirm_restart(inputs)
	if (locale === "ar") return ar_docker_modal_confirm_restart(inputs)
	if (locale === "wuu") return wuu_docker_modal_confirm_restart(inputs)
	if (locale === "mr") return mr_docker_modal_confirm_restart(inputs)
	if (locale === "nb") return nb_docker_modal_confirm_restart(inputs)
	if (locale === "fi") return fi_docker_modal_confirm_restart(inputs)
	if (locale === "da") return da_docker_modal_confirm_restart(inputs)
	if (locale === "et") return et_docker_modal_confirm_restart(inputs)
	if (locale === "lv") return lv_docker_modal_confirm_restart(inputs)
	if (locale === "lt") return lt_docker_modal_confirm_restart(inputs)
	if (locale === "pl") return pl_docker_modal_confirm_restart(inputs)
	if (locale === "de") return de_docker_modal_confirm_restart(inputs)
	if (locale === "nl") return nl_docker_modal_confirm_restart(inputs)
	if (locale === "fr") return fr_docker_modal_confirm_restart(inputs)
	if (locale === "it") return it_docker_modal_confirm_restart(inputs)
	if (locale === "hu") return hu_docker_modal_confirm_restart(inputs)
	if (locale === "cs") return cs_docker_modal_confirm_restart(inputs)
	if (locale === "ro") return ro_docker_modal_confirm_restart(inputs)
	if (locale === "el") return el_docker_modal_confirm_restart(inputs)
	if (locale === "bg") return bg_docker_modal_confirm_restart(inputs)
	if (locale === "hr") return hr_docker_modal_confirm_restart(inputs)
	if (locale === "sr") return sr_docker_modal_confirm_restart(inputs)
	if (locale === "sk") return sk_docker_modal_confirm_restart(inputs)
	if (locale === "sl") return sl_docker_modal_confirm_restart(inputs)
	if (locale === "ko") return ko_docker_modal_confirm_restart(inputs)
	return en_docker_modal_confirm_restart(inputs)
});