/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Appform_Helpscale2Inputs */

const en_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const sv_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const uk_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const zh_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const es_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const hi_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const pt_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const bn_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const ru_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const ja_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const pa_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const vi_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const yue_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const tr_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const ar_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const wuu_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const mr_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const nb_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const fi_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const da_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const et_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const lv_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const lt_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const pl_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const de_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const nl_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const fr_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const it_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const hu_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const cs_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const ro_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const el_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const bg_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const hr_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const sr_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const sk_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

const sl_appform_helpscale2 = /** @type {(inputs: Appform_Helpscale2Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode.`)
};

/**
* | output |
* | --- |
* | "Zoom level for the embedded iframe. Useful for apps with small or large UIs. Only applies to iframe open mode." |
*
* @param {Appform_Helpscale2Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }} options
* @returns {LocalizedString}
*/
const appform_helpscale2 = /** @type {((inputs?: Appform_Helpscale2Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Appform_Helpscale2Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "pa" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_appform_helpscale2(inputs)
	if (locale === "sv") return sv_appform_helpscale2(inputs)
	if (locale === "uk") return uk_appform_helpscale2(inputs)
	if (locale === "zh") return zh_appform_helpscale2(inputs)
	if (locale === "es") return es_appform_helpscale2(inputs)
	if (locale === "hi") return hi_appform_helpscale2(inputs)
	if (locale === "pt") return pt_appform_helpscale2(inputs)
	if (locale === "bn") return bn_appform_helpscale2(inputs)
	if (locale === "ru") return ru_appform_helpscale2(inputs)
	if (locale === "ja") return ja_appform_helpscale2(inputs)
	if (locale === "pa") return pa_appform_helpscale2(inputs)
	if (locale === "vi") return vi_appform_helpscale2(inputs)
	if (locale === "yue") return yue_appform_helpscale2(inputs)
	if (locale === "tr") return tr_appform_helpscale2(inputs)
	if (locale === "ar") return ar_appform_helpscale2(inputs)
	if (locale === "wuu") return wuu_appform_helpscale2(inputs)
	if (locale === "mr") return mr_appform_helpscale2(inputs)
	if (locale === "nb") return nb_appform_helpscale2(inputs)
	if (locale === "fi") return fi_appform_helpscale2(inputs)
	if (locale === "da") return da_appform_helpscale2(inputs)
	if (locale === "et") return et_appform_helpscale2(inputs)
	if (locale === "lv") return lv_appform_helpscale2(inputs)
	if (locale === "lt") return lt_appform_helpscale2(inputs)
	if (locale === "pl") return pl_appform_helpscale2(inputs)
	if (locale === "de") return de_appform_helpscale2(inputs)
	if (locale === "nl") return nl_appform_helpscale2(inputs)
	if (locale === "fr") return fr_appform_helpscale2(inputs)
	if (locale === "it") return it_appform_helpscale2(inputs)
	if (locale === "hu") return hu_appform_helpscale2(inputs)
	if (locale === "cs") return cs_appform_helpscale2(inputs)
	if (locale === "ro") return ro_appform_helpscale2(inputs)
	if (locale === "el") return el_appform_helpscale2(inputs)
	if (locale === "bg") return bg_appform_helpscale2(inputs)
	if (locale === "hr") return hr_appform_helpscale2(inputs)
	if (locale === "sr") return sr_appform_helpscale2(inputs)
	if (locale === "sk") return sk_appform_helpscale2(inputs)
	return sl_appform_helpscale2(inputs)
});
export { appform_helpscale2 as "appForm_helpScale" }