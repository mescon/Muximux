/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Onboarding_Dockerhint1Inputs */

const en_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Let Muximux scan your daemon and offer to import running containers as apps (auto-filled name, icon, port, URL). Enable it now and finish the import in Settings -> Discovery after onboarding.`)
};

const sv_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Låt Muximux skanna din daemon och erbjuda att importera körande containrar som appar (automatiskt ifyllt namn, ikon, port, URL). Aktivera det nu och slutför importen under Inställningar -> Upptäckt efter introduktionen.`)
};

const uk_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Дозвольте Muximux просканувати ваш daemon і запропонувати імпорт запущених контейнерів як застосунків (автоматично заповнені назва, іконка, порт, URL). Увімкніть це зараз і завершіть імпорт у Налаштування -> Виявлення після початкового налаштування.`)
};

const zh_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`让 Muximux 扫描你的守护进程，并将正在运行的容器导入为应用（自动填充名称、图标、端口和 URL）。立即启用，并在引导完成后于「设置 -> 发现」中完成导入。`)
};

const es_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Deja que Muximux analice tu daemon y te ofrezca importar los contenedores en ejecución como apps (nombre, icono, puerto y URL autocompletados). Actívalo ahora y termina la importación en Ajustes -> Descubrimiento tras la configuración inicial.`)
};

const hi_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Muximux को अपने डेमॉन को स्कैन करने दें और चल रहे कंटेनरों को ऐप्स के रूप में आयात करने का सुझाव दें (नाम, आइकन, पोर्ट, URL स्वतः भरे जाएंगे)। इसे अभी सक्षम करें और ऑनबोर्डिंग के बाद सेटिंग्स -> डिस्कवरी में आयात पूरा करें।`)
};

const pt_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Deixa o Muximux analisar o teu daemon e propor importar os contentores em execução como apps (nome, ícone, porta e URL preenchidos automaticamente). Ativa-o agora e conclui a importação em Definições -> Deteção após a configuração inicial.`)
};

const bn_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Muximux-কে আপনার ডেমন স্ক্যান করতে দিন এবং চলমান কন্টেইনারগুলো অ্যাপ হিসেবে ইম্পোর্ট করার প্রস্তাব দিতে দিন (নাম, আইকন, পোর্ট, URL স্বয়ংক্রিয়ভাবে পূরণ হবে)। এখনই সক্ষম করুন এবং অনবোর্ডিং শেষে সেটিংস -> ডিসকভারি-তে ইম্পোর্ট সম্পন্ন করুন।`)
};

const ru_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Позвольте Muximux сканировать ваш демон и предложить импортировать запущенные контейнеры как приложения (автозаполнение имени, иконки, порта, URL). Включите это сейчас и завершите импорт в Настройки -> Обнаружение после настройки.`)
};

const ja_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Muximux にデーモンをスキャンさせ、実行中のコンテナをアプリとしてインポートできます（名前、アイコン、ポート、URL を自動入力）。今すぐ有効にして、オンボーディング後に「設定 -> ディスカバリー」でインポートを完了してください。`)
};

const vi_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Cho phép Muximux quét daemon của bạn và đề xuất nhập các container đang chạy thành ứng dụng (tự động điền tên, biểu tượng, cổng, URL). Bật ngay và hoàn tất việc nhập trong Cài đặt -> Khám phá sau khi thiết lập ban đầu.`)
};

const yue_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`等 Muximux 掃描你嘅 daemon，將運行緊嘅容器以應用程式形式匯入（自動填好名稱、圖示、連接埠、URL）。而家就開啟佢，初始設定完成之後喺「設定 -> 發現」度完成匯入。`)
};

const tr_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Muximux'un daemon'ınızı taramasına ve çalışan kapsayıcıları uygulama olarak içe aktarmayı önermesine izin verin (ad, simge, port ve URL otomatik doldurulur). Şimdi etkinleştirin ve içe aktarmayı kurulumdan sonra Settings -> Discovery bölümünde tamamlayın.`)
};

const ar_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`دع Muximux يفحص خدمة Docker لديك ويعرض استيراد الحاويات قيد التشغيل كتطبيقات (مع تعبئة تلقائية للاسم والأيقونة والمنفذ وعنوان URL). فعّلها الآن وأكمل الاستيراد من الإعدادات (Settings -> Discovery) بعد الإعداد الأولي.`)
};

const wuu_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`让 Muximux 扫描你个守护进程，把运行中个容器作为应用导进来（名称、图标、端口、URL 自动填好）。现在开启，初始设置好之后到“设置 -> 发现”里厢完成导入。`)
};

const mr_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Muximux ला तुमचा डीमन स्कॅन करू द्या आणि चालू असलेले कंटेनर अॅप्स म्हणून आयात करण्याचा पर्याय देऊ द्या (नाव, आयकॉन, पोर्ट, URL आपोआप भरले जातील). आता सक्षम करा आणि ऑनबोर्डिंगनंतर सेटिंग्ज -> डिस्कव्हरी मध्ये आयात पूर्ण करा.`)
};

const nb_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`La Muximux skanne daemonen din og tilby å importere kjørende containere som apper (automatisk utfylt navn, ikon, port, URL). Aktiver det nå, og fullfør importen under Innstillinger -> Oppdagelse etter onboarding.`)
};

const fi_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Anna Muximuxin skannata daemonisi ja tarjota käynnissä olevien säiliöiden tuontia sovelluksiksi (automaattisesti täytetty nimi, kuvake, portti, URL). Ota se käyttöön nyt ja viimeistele tuonti kohdassa Asetukset -> Tunnistus käyttöönoton jälkeen.`)
};

const da_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lad Muximux scanne din daemon og tilbyde at importere kørende containere som apps (automatisk udfyldt navn, ikon, port, URL). Aktiver det nu, og afslut importen under Indstillinger -> Registrering efter onboarding.`)
};

const et_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lase Muximuxil oma deemonit skannida ja pakkuda töötavate konteinerite importimist rakendustena (automaatselt täidetud nimi, ikoon, port, URL). Luba see kohe ja lõpeta import pärast seadistamist menüüs Seaded -> Tuvastus.`)
};

const lv_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Ļaujiet Muximux skenēt jūsu dēmonu un piedāvāt importēt darbojošos konteinerus kā lietotnes (automātiski aizpildīts nosaukums, ikona, ports, URL). Iespējojiet to tagad un pabeidziet importēšanu sadaļā Iestatījumi -> Atklāšana pēc sākotnējās iestatīšanas.`)
};

const lt_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Leiskite Muximux nuskaityti jūsų demoną ir pasiūlyti importuoti veikiančius konteinerius kaip programas (automatiškai užpildytas pavadinimas, piktograma, prievadas, URL). Įjunkite tai dabar ir užbaikite importavimą skiltyje Nustatymai -> Aptikimas po pradinės sąrankos.`)
};

const pl_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pozwól Muximux przeskanować demona i zaproponować import działających kontenerów jako aplikacji (automatycznie wypełniona nazwa, ikona, port, adres URL). Włącz to teraz i dokończ import w Ustawienia -> Wykrywanie po zakończeniu konfiguracji.`)
};

const de_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lassen Sie Muximux Ihren Daemon scannen und laufende Container als Apps importieren (automatisch ausgefüllter Name, Symbol, Port, URL). Aktivieren Sie es jetzt und schließen Sie den Import nach dem Onboarding unter Einstellungen -> Erkennung ab.`)
};

const nl_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Laat Muximux je daemon scannen en draaiende containers als apps importeren (automatisch ingevulde naam, pictogram, poort, URL). Schakel het nu in en voltooi de import na de onboarding via Instellingen -> Detectie.`)
};

const fr_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Laisse Muximux analyser ton démon et te proposer d'importer les conteneurs en cours d'exécution en tant qu'apps (nom, icône, port et URL préremplis). Active-le maintenant et termine l'import dans Paramètres -> Découverte après l'intégration.`)
};

const it_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lascia che Muximux analizzi il tuo daemon e ti proponga di importare i container in esecuzione come app (nome, icona, porta e URL precompilati). Attivalo ora e completa l'importazione in Impostazioni -> Rilevamento dopo la configurazione iniziale.`)
};

const hu_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Engedje, hogy a Muximux átvizsgálja a démont, és felajánlja a futó konténerek alkalmazásként való importálását (automatikusan kitöltött név, ikon, port, URL). Engedélyezze most, és fejezze be az importálást a Beállítások -> Felfedezés menüben a beállítás után.`)
};

const cs_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Nechte Muximux prohledat vašeho démona a nabídnout import běžících kontejnerů jako aplikací (automaticky vyplněný název, ikona, port, URL). Povolte to nyní a dokončete import v Nastavení -> Vyhledávání po dokončení úvodního nastavení.`)
};

const ro_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Lasă Muximux să scaneze daemonul tău și să îți propună importarea containerelor în execuție ca aplicații (nume, pictogramă, port și URL completate automat). Activează-l acum și finalizează importul în Setări -> Descoperire după configurarea inițială.`)
};

const el_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Αφήστε το Muximux να σαρώσει τον daemon σας και να προτείνει την εισαγωγή των ενεργών containers ως εφαρμογές (αυτόματη συμπλήρωση ονόματος, εικονιδίου, θύρας, URL). Ενεργοποιήστε το τώρα και ολοκληρώστε την εισαγωγή στο Ρυθμίσεις -> Ανίχνευση μετά την αρχική ρύθμιση.`)
};

const bg_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Позволете на Muximux да сканира вашия daemon и да предложи импортиране на работещите контейнери като приложения (автоматично попълнени име, икона, порт, URL). Активирайте го сега и завършете импортирането в Настройки -> Откриване след първоначалната настройка.`)
};

const hr_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Dopustite Muximuxu da pregleda vaš daemon i ponudi uvoz pokrenutih kontejnera kao aplikacija (automatski popunjeni naziv, ikona, port, URL). Omogućite to sada i dovršite uvoz u Postavke -> Otkrivanje nakon uvodnog postavljanja.`)
};

const sr_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Dozvolite Muximuxu da skenira vaš daemon i ponudi uvoz pokrenutih kontejnera kao aplikacija (automatski popunjeni naziv, ikona, port, URL). Omogućite to sada i dovršite uvoz u Podešavanja -> Otkrivanje nakon uvodnog podešavanja.`)
};

const sk_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Nechajte Muximux prehľadať vášho démona a ponúknuť import bežiacich kontajnerov ako aplikácií (automaticky vyplnený názov, ikona, port, URL). Povoľte to teraz a dokončite import v Nastavenia -> Vyhľadávanie po dokončení úvodného nastavenia.`)
};

const sl_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Naj Muximux pregleda vaš demon in ponudi uvoz delujočih vsebnikov kot aplikacij (samodejno izpolnjeno ime, ikona, vrata, URL). Omogočite zdaj in dokončajte uvoz v Nastavitve -> Odkrivanje po zaključeni uvodni nastavitvi.`)
};

const ko_onboarding_dockerhint1 = /** @type {(inputs: Onboarding_Dockerhint1Inputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Muximux가 데몬을 스캔하여 실행 중인 컨테이너를 앱으로 가져오도록 제안합니다 (이름, 아이콘, 포트, URL 자동 입력). 지금 활성화하고 온보딩 후 설정 -> 검색에서 가져오기를 완료하세요.`)
};

/**
* | output |
* | --- |
* | "Let Muximux scan your daemon and offer to import running containers as apps (auto-filled name, icon, port, URL). Enable it now and finish the import in Setti..." |
*
* @param {Onboarding_Dockerhint1Inputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
const onboarding_dockerhint1 = /** @type {((inputs?: Onboarding_Dockerhint1Inputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Onboarding_Dockerhint1Inputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_onboarding_dockerhint1(inputs)
	if (locale === "sv") return sv_onboarding_dockerhint1(inputs)
	if (locale === "uk") return uk_onboarding_dockerhint1(inputs)
	if (locale === "zh") return zh_onboarding_dockerhint1(inputs)
	if (locale === "es") return es_onboarding_dockerhint1(inputs)
	if (locale === "hi") return hi_onboarding_dockerhint1(inputs)
	if (locale === "pt") return pt_onboarding_dockerhint1(inputs)
	if (locale === "bn") return bn_onboarding_dockerhint1(inputs)
	if (locale === "ru") return ru_onboarding_dockerhint1(inputs)
	if (locale === "ja") return ja_onboarding_dockerhint1(inputs)
	if (locale === "vi") return vi_onboarding_dockerhint1(inputs)
	if (locale === "yue") return yue_onboarding_dockerhint1(inputs)
	if (locale === "tr") return tr_onboarding_dockerhint1(inputs)
	if (locale === "ar") return ar_onboarding_dockerhint1(inputs)
	if (locale === "wuu") return wuu_onboarding_dockerhint1(inputs)
	if (locale === "mr") return mr_onboarding_dockerhint1(inputs)
	if (locale === "nb") return nb_onboarding_dockerhint1(inputs)
	if (locale === "fi") return fi_onboarding_dockerhint1(inputs)
	if (locale === "da") return da_onboarding_dockerhint1(inputs)
	if (locale === "et") return et_onboarding_dockerhint1(inputs)
	if (locale === "lv") return lv_onboarding_dockerhint1(inputs)
	if (locale === "lt") return lt_onboarding_dockerhint1(inputs)
	if (locale === "pl") return pl_onboarding_dockerhint1(inputs)
	if (locale === "de") return de_onboarding_dockerhint1(inputs)
	if (locale === "nl") return nl_onboarding_dockerhint1(inputs)
	if (locale === "fr") return fr_onboarding_dockerhint1(inputs)
	if (locale === "it") return it_onboarding_dockerhint1(inputs)
	if (locale === "hu") return hu_onboarding_dockerhint1(inputs)
	if (locale === "cs") return cs_onboarding_dockerhint1(inputs)
	if (locale === "ro") return ro_onboarding_dockerhint1(inputs)
	if (locale === "el") return el_onboarding_dockerhint1(inputs)
	if (locale === "bg") return bg_onboarding_dockerhint1(inputs)
	if (locale === "hr") return hr_onboarding_dockerhint1(inputs)
	if (locale === "sr") return sr_onboarding_dockerhint1(inputs)
	if (locale === "sk") return sk_onboarding_dockerhint1(inputs)
	if (locale === "sl") return sl_onboarding_dockerhint1(inputs)
	return ko_onboarding_dockerhint1(inputs)
});
export { onboarding_dockerhint1 as "onboarding_dockerHint" }