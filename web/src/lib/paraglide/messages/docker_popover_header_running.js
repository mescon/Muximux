/* eslint-disable */
import { getLocale, experimentalStaticLocale } from '../runtime.js';

/** @typedef {import('../runtime.js').LocalizedString} LocalizedString */

/** @typedef {{}} Docker_Popover_Header_RunningInputs */

const en_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Running`)
};

const sv_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Körs`)
};

const uk_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Запущено`)
};

const zh_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`运行中`)
};

const es_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`En ejecución`)
};

const hi_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`चल रहा है`)
};

const pt_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Em execução`)
};

const bn_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`চলছে`)
};

const ru_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Работает`)
};

const ja_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`実行中`)
};

const vi_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Đang chạy`)
};

const yue_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`運行中`)
};

const tr_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Çalışıyor`)
};

const ar_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`قيد التشغيل`)
};

const wuu_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`运行中`)
};

const mr_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`चालू आहे`)
};

const nb_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Kjører`)
};

const fi_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Käynnissä`)
};

const da_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Kører`)
};

const et_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Töötab`)
};

const lv_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Darbojas`)
};

const lt_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Veikia`)
};

const pl_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Działa`)
};

const de_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Läuft`)
};

const nl_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Actief`)
};

const fr_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`En cours d'exécution`)
};

const it_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`In esecuzione`)
};

const hu_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Fut`)
};

const cs_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Běží`)
};

const ro_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`În execuție`)
};

const el_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Σε εκτέλεση`)
};

const bg_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Работещ`)
};

const hr_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pokrenut`)
};

const sr_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Pokrenut`)
};

const sk_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Beží`)
};

const sl_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`Deluje`)
};

const ko_docker_popover_header_running = /** @type {(inputs: Docker_Popover_Header_RunningInputs) => LocalizedString} */ () => {
	return /** @type {LocalizedString} */ (`실행 중`)
};

/**
* | output |
* | --- |
* | "Running" |
*
* @param {Docker_Popover_Header_RunningInputs} inputs
* @param {{ locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }} options
* @returns {LocalizedString}
*/
export const docker_popover_header_running = /** @type {((inputs?: Docker_Popover_Header_RunningInputs, options?: { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }) => LocalizedString) & import('../runtime.js').MessageMetadata<Docker_Popover_Header_RunningInputs, { locale?: "en" | "sv" | "uk" | "zh" | "es" | "hi" | "pt" | "bn" | "ru" | "ja" | "vi" | "yue" | "tr" | "ar" | "wuu" | "mr" | "nb" | "fi" | "da" | "et" | "lv" | "lt" | "pl" | "de" | "nl" | "fr" | "it" | "hu" | "cs" | "ro" | "el" | "bg" | "hr" | "sr" | "sk" | "sl" | "ko" }, {}>} */ ((inputs = {}, options = {}) => {
	const locale = experimentalStaticLocale ?? options.locale ?? getLocale()
	if (locale === "en") return en_docker_popover_header_running(inputs)
	if (locale === "sv") return sv_docker_popover_header_running(inputs)
	if (locale === "uk") return uk_docker_popover_header_running(inputs)
	if (locale === "zh") return zh_docker_popover_header_running(inputs)
	if (locale === "es") return es_docker_popover_header_running(inputs)
	if (locale === "hi") return hi_docker_popover_header_running(inputs)
	if (locale === "pt") return pt_docker_popover_header_running(inputs)
	if (locale === "bn") return bn_docker_popover_header_running(inputs)
	if (locale === "ru") return ru_docker_popover_header_running(inputs)
	if (locale === "ja") return ja_docker_popover_header_running(inputs)
	if (locale === "vi") return vi_docker_popover_header_running(inputs)
	if (locale === "yue") return yue_docker_popover_header_running(inputs)
	if (locale === "tr") return tr_docker_popover_header_running(inputs)
	if (locale === "ar") return ar_docker_popover_header_running(inputs)
	if (locale === "wuu") return wuu_docker_popover_header_running(inputs)
	if (locale === "mr") return mr_docker_popover_header_running(inputs)
	if (locale === "nb") return nb_docker_popover_header_running(inputs)
	if (locale === "fi") return fi_docker_popover_header_running(inputs)
	if (locale === "da") return da_docker_popover_header_running(inputs)
	if (locale === "et") return et_docker_popover_header_running(inputs)
	if (locale === "lv") return lv_docker_popover_header_running(inputs)
	if (locale === "lt") return lt_docker_popover_header_running(inputs)
	if (locale === "pl") return pl_docker_popover_header_running(inputs)
	if (locale === "de") return de_docker_popover_header_running(inputs)
	if (locale === "nl") return nl_docker_popover_header_running(inputs)
	if (locale === "fr") return fr_docker_popover_header_running(inputs)
	if (locale === "it") return it_docker_popover_header_running(inputs)
	if (locale === "hu") return hu_docker_popover_header_running(inputs)
	if (locale === "cs") return cs_docker_popover_header_running(inputs)
	if (locale === "ro") return ro_docker_popover_header_running(inputs)
	if (locale === "el") return el_docker_popover_header_running(inputs)
	if (locale === "bg") return bg_docker_popover_header_running(inputs)
	if (locale === "hr") return hr_docker_popover_header_running(inputs)
	if (locale === "sr") return sr_docker_popover_header_running(inputs)
	if (locale === "sk") return sk_docker_popover_header_running(inputs)
	if (locale === "sl") return sl_docker_popover_header_running(inputs)
	return ko_docker_popover_header_running(inputs)
});